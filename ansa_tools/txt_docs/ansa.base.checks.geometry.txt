# Module: ansa.base.checks.geometry

CollapsedCons
ansa.base.checks.geometry.CollapsedCons() -> Check
This function creates an appropriate checks object, that when executed, will perform the same checks that are performed by the relevant ANSA Check.
Returns: Returns the requested ‘base.Check’ object.
Example:
import ansa
from ansa import base

def main():
    obj = base.checks.geometry.CollapsedCons()
    obj.execute()


Cracks
ansa.base.checks.geometry.Cracks() -> Check
This function creates an appropriate Check object, that when executed, will perform the same checks that are performed by the relevant ANSA Check.
Returns: Returns the created ‘base.Check’ object.

NeedleFaces
ansa.base.checks.geometry.NeedleFaces() -> Check
This function creates an appropriate Check object, that when executed, willperform the same checks that are performed by the relevant ANSA Check.
Attributes and default values:
-show_warnings: False
Returns: Returns the created ‘base.Check’ object.
Example:
import ansa
from ansa import base

def main():
    obj = base.checks.geometry.NeedleFaces()
    obj.show_warnings = True
    obj.execute()


OverlapFaces
ansa.base.checks.geometry.OverlapFaces() -> Check
This function creates an appropriate Check object, that when executed, will perform the same checks that are performed by the relevant ANSA Check.
Returns: Returns the created ‘base.Check’ object.
Example:
import ansa
from ansa import base

def main():
    obj = base.checks.geometry.OverlapFaces()
    obj.execute()


SingleCons
ansa.base.checks.geometry.SingleCons() -> Check
This function creates an appropriate Check object, that when executed, willperform the same checks that are performed by the relevant ANSA Check.
Returns: Returns the created ‘base.Check’ object.
Example:
import ansa
from ansa import base

def main():
    obj = base.checks.geometry.SingleCons()
    obj.execute()


TripleCons
ansa.base.checks.geometry.TripleCons() -> Check
This function creates an appropriate Check object, that when executed, will perform the same checks that are performed by the relevant ANSA Check.
Returns: Returns the created ‘base.Check’ object.
Example:
import ansa
from ansa import base

def main():
    obj = base.checks.geometry.TripleCons()
    obj.execute()


UncheckedFaces
ansa.base.checks.geometry.UncheckedFaces() -> Check
This function creates an appropriate Check object, that when executed, willperform the same checks that are performed by the relevant ANSA Check.
Returns: Returns the created ‘base.Check’ object.
Example:
import ansa
from ansa import base

def main():
    obj = base.checks.geometry.UncheckedFaces()
    obj.execute()


unmeshedMacros
ansa.base.checks.geometry.unmeshedMacros() -> Check
This function creates an appropriate Check object, that when executed, will
perform the same checks that are performed by the relevant ANSA Check.
Returns: Returns the created ‘base.Check’ object.
Example:
import ansa
from ansa import base

def main():
    obj = base.checks.geometry.unmeshedMacros()
    obj.execute()

