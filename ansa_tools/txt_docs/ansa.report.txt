# Module: ansa.report

Audio
ansa.report.Audio(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
An audio shape in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    # Second handler for the same audio using constructor
    audio2 = report.Audio(
        report_name=rep.name, slide_name=slide.name, shape_name=audio.name
    )
    print(audio2)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    rep = audio.get_report()
    print(rep)
    print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    slide = audio.get_slide()
    print(slide)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    position = audio.get_position()
    print(position)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    position = (0.5, 0.5)
    ret = audio.set_position(position)  # Middle of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    size = audio.get_size()
    print(size)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    position = (0.25, 0.25)
    ret = audio.set_size(position)  # A quarter of the size of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    ret = audio.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    ret = audio.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    ret = audio.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    ret = audio.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    name = "audio name"
    ret = audio.set_name(name)
    print(ret)
    print(audio.name)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    shape = slide.add_audio(filename)
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    ret = shape.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_audio("audio1.mp2")
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    shape.add_tag(tag)
    ret = shape.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    shape = slide.add_audio(filename)
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    tag2 = report.CreateReportTag(name="tag_name2", value="tag_value2")
    shape.add_tag(tag)
    shape.add_tag(tag2)
    tags = shape.get_tags()
    for tag in tags:
        print(tag)


if __name__ == "__main__":
    main()
# method: set_file
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    ret = audio.set_file(filename)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_file
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    file = audio.get_file()
    print(file)


if __name__ == "__main__":
    main()
# method: set_embed
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    embed = False
    ret = audio.set_embed(embed)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_embed
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    audio = slide.add_audio(filename)
    embed = audio.get_embed()
    print(embed)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    shape = slide.add_audio(filename)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "audio1.mp2"
    shape = slide.add_audio(filename)
    visibility = False
    ret = shape.set_visibility(visibility)
    print(ret)
    visibility = shape.get_visibility()
    print(visibility)


if __name__ == "__main__":
    main()

Audio.add_tag
ansa.report.Audio.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Audio.bring_forward
ansa.report.Audio.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

Audio.bring_to_front
ansa.report.Audio.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

Audio.get_embed
ansa.report.Audio.get_embed() -> bool
Whether the audio is embedded or linked.
Returns: bool True if the audio file is embedded. False if the audio file is linked.

Audio.get_file
ansa.report.Audio.get_file() -> str
Return the audio file used by this shape.
Returns: str Path to the audio file.

Audio.get_position
ansa.report.Audio.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

Audio.get_report
ansa.report.Audio.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

Audio.get_size
ansa.report.Audio.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Audio.get_slide
ansa.report.Audio.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

Audio.get_tags
ansa.report.Audio.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Audio.get_visibility
ansa.report.Audio.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Audio.remove_tag
ansa.report.Audio.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Audio.send_backward
ansa.report.Audio.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

Audio.send_to_back
ansa.report.Audio.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

Audio.set_embed
ansa.report.Audio.set_embed(embed: bool) -> bool
Define whether the audio file will be embedded or linked on export.
- embed (bool): True if the audio file is embedded. False if the audio file is linked. Returns -------
Returns: bool

Audio.set_file
ansa.report.Audio.set_file(filename: str) -> bool
Set the displayed audio file.
- filename (str): Path to an audio file. Returns -------
Returns: bool

Audio.set_name
ansa.report.Audio.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

Audio.set_position
ansa.report.Audio.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Audio.set_size
ansa.report.Audio.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Audio.set_visibility
ansa.report.Audio.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

CreateReport
ansa.report.CreateReport(name:str) -> Report
Creates a report composer window with the provided name, or a unique generated name if one is not provided. Returns None if a name is provided and a report with that name already exists.
- name (str, optional): Name of the new report composer window.
Returns: Upon success returns an object of type meta.report.Report
Upon failure returns None.

CreateReportFilter
ansa.report.CreateReportFilter(type:str, name:str, operator:str, value:str, value2:str) -> ReportFilter
Creates a report filter.
- type (str): Available values are listed below. Note that the ‘tag’ type also requires the rest of the arguments to be specified, a name, operator, and at least one value (depending on the operator).
- name (str, optional): The tag name to search for.
- operator (str, optional): Available values are listed below, note that some operators require a value2 argument as well.
- value (str, optional): The tag value that is matched against the value of the tags in the report files using the specified operator.
- value2 (str, optional): A second optional value required by operators  ‘between’ and ‘not_between’.
Returns: meta.report.ReportFilter
Example:
# PYTHON script
import ansa
from ansa import report

def main():
    directory = "../"
    type = "presentation"
    presFilter = report.CreateReportFilter("presentation")
    # OR
    type = "tag"
    name = "tag1"
    operator = "equal"
    value = "value1"
    tagFilter = report.CreateReportFilter(type, name, operator, value)

    presFilter.add_child(tagFilter)
    print(presFilter)
    print(presFilter.get_children())
    print(presFilter.search(directory))

if __name__ == "__main__":
    main()


CreateReportTag
ansa.report.CreateReportTag(name:str, value:str) -> ReportTag
Creates a report tag.
- name (str): 
- value (str): 
Returns: meta.report.ReportTag
Example:
# PYTHON script
import ansa
from ansa import report

def main():
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    print(tag)

if __name__ == "__main__":
    main()


DeleteReport
ansa.report.DeleteReport(report:ansa.report.Report|str) -> bool
Deletes the given report from META. This action invalidates the object.
- report (Report | str): Either a report name as a string or a meta.report.Report object.
Returns: Upon success returns True
Upon failure returns False.
Example:
# PYTHON script
import ansa
from ansa import report

def main():
    # Create new report
    r = report.CreateReport()
    # Delete report
    ret = report.DeleteReport(r)
    print(ret)

if __name__ == "__main__":
    main()


Ellipse
ansa.report.Ellipse(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
An ellipse shape in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    # name = 'Ellipse 1'
    # ellipse = slide.add_ellipse(name)
    print(ellipse)
    # Second handler for the same ellipse using constructor
    ellipse2 = report.Ellipse(
        report_name=rep.name, slide_name=slide.name, shape_name=ellipse.name
    )
    print(ellipse2)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    name = "ellipse name"
    ret = ellipse.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()

    r = ellipse.get_report()
    print(r.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()

    slide = ellipse.get_slide()
    print(slide.name)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()

    pos = ellipse.get_position()
    # pos = ellipse.get_position(unit = 'pixel')
    print(pos)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()

    pos = (0.5, 0.5)
    ret = ellipse.set_position(pos)  # Middle of the slide
    print(ret)

    pos = (50, 20)
    ret = ellipse.set_position(pos, unit="mm")
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    size = ellipse.get_size()
    print(size)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()

    size = (0.25, 0.25)
    ret = ellipse.set_size(size)  # A quarter of the size of the slide
    print(ret)

    size = (200, 400)
    ret = ellipse.set_size(size, unit="pixel")
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    ret = ellipse.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    ret = ellipse.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    ret = ellipse.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    ret = ellipse.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    angle = 90
    ret = ellipse.set_rotation(angle)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()

    rot = ellipse.get_rotation()
    print(rot)


if __name__ == "__main__":
    main()
# method: set_line_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    fill = "no_line"
    # fill = 'solid_line'
    ret = ellipse.set_line_style(fill)
    print(ret)
    c = windows.Color(name="color1", r=0, g=255, b=0, a=255)
    ret = ellipse.set_line_style(fill, color=c)
    # ret = ellipse.set_line_style(fill, color=c, width = 10)
    # ret = ellipse.set_line_style(fill, color=c, width = 10, dash = 'sys_dash_dot_dot_line')
    # ret = ellipse.set_line_style(fill, color=c, width = 10, dash = 'custom_dash_line', custom_dash_pattern = '111000')
    # ret = ellipse.set_line_style(fill, color=c, width = 10, dash = 'custom_dash_line', custom_dash_pattern = '111000', cap = 'round_cap')
    # ret = ellipse.set_line_style(fill, color=c, width = 10, dash = 'custom_dash_line', custom_dash_pattern = '111000', cap = 'round_cap', join = 'miter_join')
    print(ret)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    fill = "no_fill"
    # fill = 'solid_fill'
    # fill = 'picture_fill'
    ret = ellipse.set_fill(fill)
    print(ret)
    c = windows.Color(name="color1", r=255, g=0, b=0, a=50)
    ret = ellipse.set_fill(fill, color=c)
    # ret = ellipse.set_fill("picture_fill", filename = "/home/images/Image1.png")
    print(ret)


if __name__ == "__main__":
    main()
# method: set_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()

    pos = (0.25, 0.25)
    ellipse.set_size(pos)
    text = "my_text"
    ret = ellipse.set_text(text)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    text = "my_text"
    ret = ellipse.set_text(text)
    print(ret)
    text = ellipse.get_text()
    # text = ellipse.get_text(html = True)
    print(text)


if __name__ == "__main__":
    main()
# method: set_text_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()

    pos = (0.25, 0.25)
    ellipse.set_size(pos)
    text = "my_text"
    ret = ellipse.set_text(text)
    ret = ellipse.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    # ret = ellipse.set_text_style(italic=True, underline=Tru, superscript=True, strike_through=True, subscript = True)
    # color = windows.Color(r = 255, g = 255, b = 0, a = 255)
    # ret = ellipse.set_text_style(text_color = color, text_align = 'left', line_spacing = 1.2, shape_align = 'top', autofit = 'no_autofit', wrap = True)
    print(ret)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = ellipse.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    tag = report.CreateReportTag("tag_name", "tag_value")
    ellipse.add_tag(tag)
    ret = ellipse.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    ellipse.add_tag(tag)
    ellipse.add_tag(tag2)
    tags = ellipse.get_tags()
    for t in tags:
        print(t.name, t.value)


if __name__ == "__main__":
    main()
# method: get_text_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_ellipse()
    shape.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    shape.set_text_style(
        italic=True,
        underline=True,
        strike_through=True,
        superscript=True,
        subscript=True,
    )
    color = windows.Color(r=255, g=255, b=0, a=255)
    shape.set_text_style(
        text_color=color,
        text_align="left",
        shape_align="top",
        autofit="no_autofit",
        wrap=True,
    )
    style = shape.get_text_style()
    print(style)

    shape2 = slide.add_ellipse()
    shape2.set_text_style(**style)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_ellipse()
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_ellipse()
    shape.set_visibility(False)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()

Ellipse.add_tag
ansa.report.Ellipse.add_tag(tag: ReportTag) -> bool
Adds a tag to the ellipse.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Ellipse.bring_forward
ansa.report.Ellipse.bring_forward() -> bool
Place ellipse in front of the shape immediately in front of it.
Returns: bool

Ellipse.bring_to_front
ansa.report.Ellipse.bring_to_front() -> bool
Place ellipse in front of the other shapes in the slide.
Returns: bool

Ellipse.get_position
ansa.report.Ellipse.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the ellipse.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

Ellipse.get_report
ansa.report.Ellipse.get_report() -> Report
Return the report that this ellipse belongs to.
Returns: Report meta.report.Report

Ellipse.get_rotation
ansa.report.Ellipse.get_rotation() -> float
Return the angle of rotation of this ellipse in degrees.
Returns: float

Ellipse.get_size
ansa.report.Ellipse.get_size(unit: str) -> Iterable[float]
Returns the width and height of the ellipse.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Ellipse.get_slide
ansa.report.Ellipse.get_slide() -> Slide
Return the slide that this ellipse belongs to.
Returns: Slide meta.report.Slide

Ellipse.get_tags
ansa.report.Ellipse.get_tags() -> Iterable[ReportTag]
Returns the ellipse tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Ellipse.get_text
ansa.report.Ellipse.get_text(html: bool) -> str
Returns the text of the ellipse.
- html (bool, optional): When True the text is returned in html formatting. The default value is False. Returns -------
Returns: str

Ellipse.get_text_style
ansa.report.Ellipse.get_text_style(position: int) -> dict
Returns the text style in the form of a python dictionary. The dictionary contains the same keys and can be applied directly to the set_text_style function. An optional position argument can be used to return the style of a specific character in the text.
- position (int, optional): Index of the character in the text whose style is returned. Indexing starts at 1. If undefined the style of the last character is returned. Returns -------
Returns: dict Python dictionary with the following text style options.font_family     stringfont_size       integerbold            booleanitalic          booleanunderline       booleanstrike_through  booleansuperscript     booleansubscript       booleantext_color      meta.windows.Colortext_align      string                        'left'                        'center'                        'right'                        'justify'shape_align     string                        'top'                        'middle'                        'bottom'                        'top_center'                        'middle_center'                        'bottom_center'autofit         string                        'no_autofit'                        'shrink_text_on_overflow'                        'resize_shape_to_fit_text'wrap            boolean

Ellipse.get_visibility
ansa.report.Ellipse.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Ellipse.remove_tag
ansa.report.Ellipse.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the ellipse.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Ellipse.send_backward
ansa.report.Ellipse.send_backward() -> bool
Place ellipse behind the shape immediately behind it.
Returns: bool

Ellipse.send_to_back
ansa.report.Ellipse.send_to_back() -> bool
Place ellipse behind the other shapes in the slide.
Returns: bool

Ellipse.set_fill
ansa.report.Ellipse.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

Ellipse.set_line_style
ansa.report.Ellipse.set_line_style(fill: str, color: windows.Color, width: float, dash: str, custom_dash_pattern: str, cap: str, join: str) -> bool
Set various line style options. All arguments are optional, any number of them can be used. The result is visible only when fill is 'solid_line'.
- fill (str, optional): The available values are: - 'no_line' - 'solid_line'
- color (windows.Color, optional): meta.windows.Color
- width (float, optional): Line width.
- dash (str, optional): One of the following predefined dash patterns: 'solid_line' 'dash_line' 'dot_line' 'dash_dot_line' 'lg_dash_line' 'lg_dash_dot_line' 'lg_dash_dot_dot_line' 'sys_dash_line' 'sys_dash_dot_line' 'sys_dash_dot_dot_line' 'sys_dot_line' 'custom_dash_line'
- custom_dash_pattern (str, optional): A series of 1's and 0's describing a dash pattern. For example '111000'.
- cap (str, optional): One of the following: 'flat_cap' 'round_cap' 'square_cap'
- join (str, optional): One of the following: 'bevel_join' 'miter_join' 'round_join' Returns -------
Returns: bool

Ellipse.set_name
ansa.report.Ellipse.set_name(name: str) -> bool
Set the ellipse name.
- name (str): Returns -------
Returns: bool

Ellipse.set_position
ansa.report.Ellipse.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the ellipse.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Ellipse.set_rotation
ansa.report.Ellipse.set_rotation(angle: float) -> bool
Set the angle of rotation of this ellipse in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

Ellipse.set_size
ansa.report.Ellipse.set_size(size: Iterable[float], unit: str) -> bool
Set the ellipse width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Ellipse.set_text
ansa.report.Ellipse.set_text(text: str) -> bool
Set the text. Accepts html for formatting.
- text (str): Returns -------
Returns: bool

Ellipse.set_text_style
ansa.report.Ellipse.set_text_style(font_family: str, font_size: int, bold: bool, italic: bool, underline: bool, strike_through: bool, superscript: bool, subscript: bool, text_color: windows.Color, text_align: str, shape_align: str, autofit: str, wrap: bool) -> bool
Set text style options. All arguments are optional. Any number of them can be used.
- font_family (str, optional): See corresponding setting in GUI for the available fonts.
- font_size (int, optional): See corresponding setting in GUI for size values.
- bold (bool, optional): 
- italic (bool, optional): 
- underline (bool, optional): 
- strike_through (bool, optional): 
- superscript (bool, optional): 
- subscript (bool, optional): 
- text_color (windows.Color, optional): meta.windows.Color
- text_align (str, optional): The following values are supported: 'left' 'center' 'right' 'justify'
- shape_align (str, optional): The following values are supported: 'top' 'middle' 'bottom' 'top_center' 'middle_center' 'bottom_center'
- autofit (str, optional): The following values are supported: 'no_autofit' 'shrink_text_on_overflow' 'resize_shape_to_fit_text'
- wrap (bool, optional): Wrap text option. Returns -------
Returns: bool

Ellipse.set_visibility
ansa.report.Ellipse.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

ExcelTable
ansa.report.ExcelTable(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
An excel table in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    print(table.name)
    print(table.slide_name)
    print(table.report_name)
    # Second handler for the same shape using constructor
    table2 = report.ExcelTable(
        report_name=rep.name, slide_name=slide.name, shape_name=table.name
    )
    print(table2)
    print(table2.name)
    print(table2.slide_name)
    print(table2.report_name)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = table.add_tag(tag)
    print(ret)
    tags = table.get_tags()
    for t in tags:
        print(t.name, t.value)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    ret = table.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    ret = table.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    pos = table.get_position()
    # pos = table.get_position(unit = 'pixel')
    print(pos)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    r = table.get_report()
    print(r)
    print(r.name)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    size = table.get_size()
    print(size)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()

    slide = table.get_slide()
    print(slide)
    print(slide.name)
    print(slide.report_name)


if __name__ == "__main__":
    main()
# method: get_spreadsheet
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    s = table.get_spreadsheet()
    print(s)
    print(s.name)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    table.add_tag(tag)
    table.add_tag(tag2)
    tags = table.get_tags()
    for t in tags:
        print(t.name, t.value)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    table.add_tag(tag)
    ret = table.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    ret = table.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    ret = table.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    ret = table.set_name("table name")
    print(ret)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()

    pos = (0.5, 0.5)
    ret = table.set_position(pos)  # Middle of the slide
    # u# = 'emu'
    # u = 'cm'
    # u = 'mm'
    # u = 'inch'
    # u = 'pt'
    # u = 'pc'
    # u = 'pixel'
    # u = 'norm'
    # ret = table.set_position(pos, unit = 'mm')
    print(ret)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    size = (0.25, 0.25)
    ret = table.set_size(size)  # A quarter of the size of the slide
    print(ret)

    size = (200, 400)
    ret = table.set_size(size, unit="pixel")
    print(ret)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    ret = table.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    table.set_visibility(False)
    ret = table.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()

ExcelTable.add_tag
ansa.report.ExcelTable.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

ExcelTable.bring_forward
ansa.report.ExcelTable.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

ExcelTable.bring_to_front
ansa.report.ExcelTable.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

ExcelTable.get_position
ansa.report.ExcelTable.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

ExcelTable.get_report
ansa.report.ExcelTable.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

ExcelTable.get_size
ansa.report.ExcelTable.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

ExcelTable.get_slide
ansa.report.ExcelTable.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

ExcelTable.get_spreadsheet
ansa.report.ExcelTable.get_spreadsheet() -> spreadsheet.Spreadsheet
Returns the spreadsheet behind this shape.
Returns: spreadsheet.Spreadsheet meta.spreadsheet.Spreadsheet

ExcelTable.get_tags
ansa.report.ExcelTable.get_tags() -> ReportTag
Returns the shape tags.
Returns: ReportTag List of report.ReportTag objects

ExcelTable.get_visibility
ansa.report.ExcelTable.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

ExcelTable.remove_tag
ansa.report.ExcelTable.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

ExcelTable.send_backward
ansa.report.ExcelTable.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

ExcelTable.send_to_back
ansa.report.ExcelTable.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

ExcelTable.set_name
ansa.report.ExcelTable.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

ExcelTable.set_position
ansa.report.ExcelTable.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

ExcelTable.set_size
ansa.report.ExcelTable.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

ExcelTable.set_visibility
ansa.report.ExcelTable.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

Freeform
ansa.report.Freeform(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A freeform shape in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    # Second handler for the same freeform shape using constructor
    freeform2 = report.Freeform(
        report_name=rep.name, slide_name=slide.name, shape_name=freeform.name
    )


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    name = "freeform name"
    ret = freeform.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    print(freeform.get_report())


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    print(freeform.get_slide())


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    print(freeform.get_position())


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    position = (0.5, 0.5)
    ret = freeform.set_position(position)  # Middle of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    print(freeform.get_size())


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    size = (0.25, 0.25)
    ret = freeform.set_size(size)  # A quarter of the size of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    ret = freeform.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    ret = freeform.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    ret = freeform.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    ret = freeform.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    angle = 90
    ret = freeform.set_rotation(angle)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    print(freeform.get_rotation())


if __name__ == "__main__":
    main()
# method: set_line_style
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    c = Color(name="color1", r=0, g=255, b=0, a=255)
    fill = "solid_line"
    ret = freeform.set_line_style(fill, color=c)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "solid_fill"
    ret = freeform.set_fill(fill, color=c)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    size = (0.25, 0.25)
    ret = freeform.set_size(size)
    text = "text"
    ret = freeform.set_text(text)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    freeform.set_text("some text")
    print(freeform.get_text())


if __name__ == "__main__":
    main()
# method: set_text_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    ret = freeform.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    ret = freeform.set_text_style(
        italic=True,
        underline=True,
        strike_through=True,
        superscript=True,
        subscript=True,
    )
    color = windows.Color(r=255, g=255, b=0, a=255)
    ret = freeform.set_text_style(
        text_color=color, text_align="left", shape_align="top", autofit="no_autofit"
    )
    print(ret)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_freeform()
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    ret = shape.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_freeform()
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    ret = shape.add_tag(tag)
    print(ret)
    ret = shape.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_freeform()
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    tag2 = report.CreateReportTag(name="tag_name2", value="tag_value2")
    ret = shape.add_tag(tag)
    ret = shape.add_tag(tag2)
    tags = shape.get_tags()
    for tag in tags:
        print(tag)


if __name__ == "__main__":
    main()
# method: get_points
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_freeform()
    points = [[0.1, 0.1], [0.9, 0.9]]
    shape.set_points(points)
    points = shape.get_points()
    print(points)


if __name__ == "__main__":
    main()
# method: set_points
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_freeform()
    points = [[0.1, 0.1], [0.9, 0.9]]
    ret = shape.set_points(points)
    print(ret)
    points = shape.get_points()
    print(points)


if __name__ == "__main__":
    main()
# method: get_text_style
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_freeform()
    shape.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    style = shape.get_text_style()
    print(style)
    shape2 = slide.add_freeform()
    shape2.set_text_style(**style)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_freeform()
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_freeform()
    shape.set_visibility(False)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()

Freeform.add_tag
ansa.report.Freeform.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Freeform.bring_forward
ansa.report.Freeform.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

Freeform.bring_to_front
ansa.report.Freeform.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

Freeform.get_points
ansa.report.Freeform.get_points(unit: str) -> list
Returns a list of the points of this shape. The points are optionally converted to the desired unit.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: list A list of x, y pairs in the requested units.

Freeform.get_position
ansa.report.Freeform.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

Freeform.get_report
ansa.report.Freeform.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

Freeform.get_rotation
ansa.report.Freeform.get_rotation() -> float
Return the angle of rotation of this shape in degrees.
Returns: float

Freeform.get_size
ansa.report.Freeform.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Freeform.get_slide
ansa.report.Freeform.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

Freeform.get_tags
ansa.report.Freeform.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Freeform.get_text
ansa.report.Freeform.get_text(html: bool) -> str
Returns the text of the shape.
- html (bool, optional): When True the text is returned in html formatting. The default value is False. Returns -------
Returns: str

Freeform.get_text_style
ansa.report.Freeform.get_text_style(position: int) -> dict
Returns the text style in the form of a python dictionary. The dictionary contains the same keys and can be applied directly to the set_text_style function. An optional position argument can be used to return the style of a specific character in the text.
- position (int, optional): Index of the character in the text whose style is returned. Indexing starts at 1. If undefined the style of the last character is returned. Returns -------
Returns: dict Python dictionary with the following text style options.font_family     stringfont_size       integerbold            booleanitalic          booleanunderline       booleanstrike_through  booleansuperscript     booleansubscript       booleantext_color      meta.windows.Colortext_align      string                        'left'                        'center'                        'right'                        'justify'shape_align     string                        'top'                        'middle'                        'bottom'                        'top_center'                        'middle_center'                        'bottom_center'autofit         string                        'no_autofit'                        'shrink_text_on_overflow'                        'resize_shape_to_fit_text'wrap            boolean

Freeform.get_visibility
ansa.report.Freeform.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Freeform.remove_tag
ansa.report.Freeform.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): report.ReportTag object or tag name Returns -------
Returns: bool

Freeform.send_backward
ansa.report.Freeform.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

Freeform.send_to_back
ansa.report.Freeform.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

Freeform.set_fill
ansa.report.Freeform.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

Freeform.set_line_style
ansa.report.Freeform.set_line_style(fill: str, color: windows.Color, width: float, dash: str, custom_dash_pattern: str, cap: str, join: str) -> bool
Set various line style options. All arguments are optional, any number of them can be used. The result is visible only when fill is 'solid_line'.
- fill (str, optional): The available values are: - 'no_line' - 'solid_line'
- color (windows.Color, optional): meta.windows.Color
- width (float, optional): Line width.
- dash (str, optional): One of the following predefined dash patterns: 'solid_line' 'dash_line' 'dot_line' 'dash_dot_line' 'lg_dash_line' 'lg_dash_dot_line' 'lg_dash_dot_dot_line' 'sys_dash_line' 'sys_dash_dot_line' 'sys_dash_dot_dot_line' 'sys_dot_line' 'custom_dash_line'
- custom_dash_pattern (str, optional): A series of 1's and 0's describing a dash pattern. For example '111000'.
- cap (str, optional): One of the following: 'flat_cap' 'round_cap' 'square_cap'
- join (str, optional): One of the following: 'bevel_join' 'miter_join' 'round_join' Returns -------
Returns: bool

Freeform.set_name
ansa.report.Freeform.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

Freeform.set_points
ansa.report.Freeform.set_points(points: list, unit: str) -> bool
Set the points of this shape.
- points (list): A non-empty list of pairs of x, y coordinates.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Freeform.set_position
ansa.report.Freeform.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Freeform.set_rotation
ansa.report.Freeform.set_rotation(angle: float) -> bool
Set the angle of rotation of this shape in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

Freeform.set_size
ansa.report.Freeform.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Freeform.set_text
ansa.report.Freeform.set_text(text: str) -> bool
Set the text. Accepts html for formatting.
- text (str): Returns -------
Returns: bool

Freeform.set_text_style
ansa.report.Freeform.set_text_style(font_family: str, font_size: int, bold: bool, italic: bool, underline: bool, strike_through: bool, superscript: bool, subscript: bool, text_color: windows.Color, text_align: str, shape_align: str, autofit: str, wrap: bool) -> bool
Set text style options. All arguments are optional. Any number of them can be used.
- font_family (str, optional): See corresponding setting in GUI for the available fonts.
- font_size (int, optional): See corresponding setting in GUI for size values.
- bold (bool, optional): 
- italic (bool, optional): 
- underline (bool, optional): 
- strike_through (bool, optional): 
- superscript (bool, optional): 
- subscript (bool, optional): 
- text_color (windows.Color, optional): meta.windows.Color
- text_align (str, optional): The following values are supported: 'left' 'center' 'right' 'justify'
- shape_align (str, optional): The following values are supported: 'top' 'middle' 'bottom' 'top_center' 'middle_center' 'bottom_center'
- autofit (str, optional): The following values are supported: 'no_autofit' 'shrink_text_on_overflow' 'resize_shape_to_fit_text'
- wrap (bool, optional): Wrap text option. Returns -------
Returns: bool

Freeform.set_visibility
ansa.report.Freeform.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

HeightOfSlide
ansa.report.HeightOfSlide(unit:float) -> float
This function returns the height of the report slide.
- unit (float): Refers to the unit of measurement of the height. Its possible values are:
- ‘mm’ : Millimeters
- ‘pixel’ : Pixels.
Returns: This function returns as float the height of the report slide in the given unit of measurement.
Example:
# PYTHON script
import ansa
from ansa import report

def main():
    h_mm = report.HeightOfSlide("mm")  # The height of the report slide in millimeter
    h_px = report.HeightOfSlide("pixel")  # The height of the report slide in pixels
    print("The height of the slide is " + str(h_mm) + " mm or " + str(h_px) + "px.")

if __name__ == "__main__":
    main()


Line
ansa.report.Line(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A line in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    # Second handler for the same textbox using constructor
    line2 = report.Line(
        report_name=rep.name, slide_name=slide.name, shape_name=line.name
    )
    print(line2)
    print(line2.name, line2.slide_name, line2.name)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    name = "line name"
    ret = line.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    rep = line.get_report()
    print(rep)
    print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    slide = line.get_slide()
    print(slide)
    print(slide.name)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    position = line.get_position()
    print(position)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    position = (0.5, 0.5)
    ret = line.set_position(position)  # Middle of the slide
    # u =  'emu'
    # u =  'cm'
    # u = 'mm'
    # u =  'inch'
    # u =  'pt'
    # u = 'pc'
    # u = 'pixel'
    # u =  'norm'
    # ret = line.set_position(position, unit = u)   # Middle of the slide


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    size = line.get_size()
    # u =  'emu'
    # u =  'cm'
    # u = 'mm'
    # u =  'inch'
    # u =  'pt'
    # u = 'pc'
    # u = 'pixel'
    # u =  'norm'
    # size = line.get_size(unit = u)
    print(size)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    size = (0.25, 0.25)
    ret = line.set_size(size)  # A quarter of the size of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    ret = line.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    ret = line.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    ret = line.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    ret = line.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    angle = 90
    ret = line.set_rotation(angle)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    rot = line.get_rotation()
    print(rot)


if __name__ == "__main__":
    main()
# method: set_line_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    c = windows.Color(name="color1", r=0, g=255, b=0, a=255)
    ret = line.set_line_style(fill="solid_line", color=c, width=1.0, dash="solid_line")
    # ret = line.set_line_style(custom_dash_pattern = '111000', cap = 'round_cap', join = 'bevel_join')
    print(ret)


if __name__ == "__main__":
    main()
# method: get_p1_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    position = line.get_p1_position()
    # u =  'emu'
    # u =  'cm'
    # u = 'mm'
    # u =  'inch'
    # u =  'pt'
    # u = 'pc'
    # u = 'pixel'
    # u =  'norm'
    # position = line.get_p1_position(unit = u)
    print(position)


if __name__ == "__main__":
    main()
# method: get_p2_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    position = line.get_p2_position()
    # u =  'emu'
    # u =  'cm'
    # u = 'mm'
    # u =  'inch'
    # u =  'pt'
    # u = 'pc'
    # u = 'pixel'
    # u =  'norm'
    # position = line.get_p2_position(unit = u)
    print(position)


if __name__ == "__main__":
    main()
# method: set_p1_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    position = (0.25, 0.25)
    ret = line.set_p1_position(position)
    # u =  'emu'
    # u =  'cm'
    # u = 'mm'
    # u =  'inch'
    # u =  'pt'
    # u = 'pc'
    # u = 'pixel'
    # u =  'norm'
    # ret = line.set_p1_position(unit = u)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_p2_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    position = (0.25, 0.25)
    ret = line.set_p2_position(position)
    # u =  'emu'
    # u =  'cm'
    # u = 'mm'
    # u =  'inch'
    # u =  'pt'
    # u = 'pc'
    # u = 'pixel'
    # u =  'norm'
    # ret = line.set_p2_position(unit = u)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_p1_arrow_style
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    ret = line.set_p1_arrow_style(type="stealth", width="large", length="large")
    print(ret)


if __name__ == "__main__":
    main()
# method: set_p2_arrow_style
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    ret = line.set_p2_arrow_style(type="stealth", width="large", length="large")
    print(ret)


if __name__ == "__main__":
    main()
# method: connect_p1
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    rect = slide.add_rectangle()
    shape = rect
    ret = line.connect_p1(shape, position="top")
    #    ret = line.connect_p1(shape, connector=1)
    print(ret)


if __name__ == "__main__":
    main()
# method: connect_p2
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    rect = slide.add_rectangle()
    shape = rect
    ret = line.connect_p2(rect, position="top")


#    ret = line.connect_p2(rect, connector=1)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_line()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)
    tags = shape.get_tags()
    for t in tags:
        print(t)
        print(t.name, t.value)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_line()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    tag2 = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)
    ret = shape.add_tag(tag2)
    print(ret)
    ret = shape.remove_tag(tag)
    print(ret)
    tags = shape.get_tags()
    for tag in tags:
        print(tag)
        print(tag.name)
        print(tag.value)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_line()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)
    ret = shape.add_tag(tag2)
    print(ret)
    tags = shape.get_tags()
    for tag in tags:
        print(tag)
        print(tag.name)
        print(tag.value)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_line()
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_line()
    shape.set_visibility(False)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()

Line.add_tag
ansa.report.Line.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Line.bring_forward
ansa.report.Line.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

Line.bring_to_front
ansa.report.Line.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

Line.connect_p1
ansa.report.Line.connect_p1(shape: object, position: str, connector: int) -> bool
Connects the first point of the line to the given shape. The specific connection can either be selected using the "position" argument or the "connector" argument.
- shape (object): The shape on which to connect the line. One of the shape types provided in the report module.
- position (str, optional): The following values are accepted. Only a subset of the available options is supported for each shape type. 'top' 'right' 'bottom' 'left' 'top_left' 'top_right' 'bottom_right' 'bottom_left'
- connector (int, optional): The accepted integer values depend on the type of shape that the line connects to. When connected to a rectangle the accepted values are 0-3. They correspond to the middle position of each side with 0 being the top, 1 the left, 2 the bottom and 3 the right side. When connected to a triangle the accepted values are 0-5. 0, 2 and 4 correspond to the corners whereas 1,3 and 5 to the middle positions of the triangle's sides. When connected to an ellipse the accepted values are 0-7, one for every 45 degrees. 0 corresponds to the top of the ellipse. Returns -------
Returns: bool

Line.connect_p2
ansa.report.Line.connect_p2(shape: object, position: str, connector: int) -> bool
Connects the second point of the line to the given shape. The specific connection can either be selected using the "position" argument or the "connector" argument.
- shape (object): The shape on which to connect the line. One of the shape types provided in the report module.
- position (str, optional): The following values are accepted. Only a subset of the available options is supported for each shape type. 'top' 'right' 'bottom' 'left' 'top_left' 'top_right' 'bottom_right' 'bottom_left'
- connector (int, optional): The accepted integer values depend on the type of shape that the line connects to. When connected to a rectangle the accepted values are 0-3. They correspond to the middle position of each side with 0 being the top, 1 the left, 2 the bottom and 3 the right side. When connected to a triangle the accepted values are 0-5. 0, 2 and 4 correspond to the corners whereas 1,3 and 5 to the middle positions of the triangle's sides. When connected to an ellipse the accepted values are 0-7, one for every 45 degrees. 0 corresponds to the top of the ellipse. Returns -------
Returns: bool

Line.get_p1_position
ansa.report.Line.get_p1_position(unit: str) -> Iterable[float]
Return the coordinates of the first point of the line.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

Line.get_p2_position
ansa.report.Line.get_p2_position(unit: str) -> Iterable[float]
Return the coordinates of the second point of the line.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

Line.get_position
ansa.report.Line.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x an y coordinates.

Line.get_report
ansa.report.Line.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

Line.get_rotation
ansa.report.Line.get_rotation() -> float
Return the angle of rotation of the shape in degrees.
Returns: float

Line.get_size
ansa.report.Line.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Line.get_slide
ansa.report.Line.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

Line.get_tags
ansa.report.Line.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Line.get_visibility
ansa.report.Line.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Line.remove_tag
ansa.report.Line.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Line.send_backward
ansa.report.Line.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

Line.send_to_back
ansa.report.Line.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

Line.set_line_style
ansa.report.Line.set_line_style(fill: str, color: windows.Color, width: float, dash: str, custom_dash_pattern: str, cap: str, join: str) -> bool
Set various line style options. All arguments are optional, any number of them can be used. The result is visible only when fill is 'solid_line'.
- fill (str, optional): The available values are: - 'no_line' - 'solid_line'
- color (windows.Color, optional): meta.windows.Color
- width (float, optional): Line width.
- dash (str, optional): One of the following predefined dash patterns: 'solid_line' 'dash_line' 'dot_line' 'dash_dot_line' 'lg_dash_line' 'lg_dash_dot_line' 'lg_dash_dot_dot_line' 'sys_dash_line' 'sys_dash_dot_line' 'sys_dash_dot_dot_line' 'sys_dot_line' 'custom_dash_line'
- custom_dash_pattern (str, optional): A series of 1's and 0's describing a dash pattern. For example '111000'.
- cap (str, optional): One of the following: 'flat_cap' 'round_cap' 'square_cap'
- join (str, optional): One of the following: 'bevel_join' 'miter_join' 'round_join' Returns -------
Returns: bool

Line.set_name
ansa.report.Line.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

Line.set_p1_arrow_style
ansa.report.Line.set_p1_arrow_style(type: str, width: str, length: str) -> bool
Provides arrow styling options.
- type (str, optional): The following values are supported: 'no_arrow' 'line_arrow' 'diamond' 'oval' 'stealth' 'triangle'
- width (str, optional): The following values are supported: 'small' 'medium' 'large'
- length (str, optional): The following values are supported: 'small' 'medium' 'large' Returns -------
Returns: bool

Line.set_p1_position
ansa.report.Line.set_p1_position(position: Iterable[float], unit: str) -> bool
Sets the position of the first point of the line.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Line.set_p2_arrow_style
ansa.report.Line.set_p2_arrow_style(type: str, width: str, length: str) -> bool
Provides arrow styling options.
- type (str, optional): The following values are supported: 'no_arrow' 'line_arrow' 'diamond' 'oval' 'stealth' 'triangle'
- width (str, optional): The following values are supported: 'small' 'medium' 'large'
- length (str, optional): The following values are supported: 'small' 'medium' 'large' Returns -------
Returns: bool

Line.set_p2_position
ansa.report.Line.set_p2_position(position: Iterable[float], unit: str) -> bool
Sets the position of the second point of the line.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Line.set_position
ansa.report.Line.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Line.set_rotation
ansa.report.Line.set_rotation(angle: float) -> bool
Set the angle of rotation of the shape in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

Line.set_size
ansa.report.Line.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Line.set_visibility
ansa.report.Line.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

Model3D
ansa.report.Model3D(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A 3d model in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    # Second handler for the same model using constructor
    model2 = report.Model3D(rep.name, slide.name, model.name)
    print(model2)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    model.set_name("model name")
    print(model.name)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    print(model.get_report())


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    print(model.get_slide())


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    print(model.get_position())


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    model.set_position((0.5, 0.5))  # Middle of the slide


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    print(model.get_size())


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    model.set_size((0.25, 0.25))  # A quarter of the size of the slide


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    model.bring_to_front()


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    model.bring_forward()


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    model.send_to_back()


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    model.send_backward()


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    model.set_rotation(90)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    print(model.get_rotation())


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_3dmodel("model.glb")
    tag = report.CreateReportTag("tag_name", "tag_value")
    shape.add_tag(tag)
    print(shape.get_tags())


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_3dmodel("model.glb")
    tag = report.CreateReportTag("tag_name", "tag_value")
    tag2 = report.CreateReportTag("tag_name2", "tag_value2")
    shape.add_tag(tag)
    shape.add_tag(tag2)
    shape.remove_tag(tag)
    print(shape.get_tags())


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_3dmodel("model.glb")
    tag = report.CreateReportTag("tag_name", "tag_value")
    tag2 = report.CreateReportTag("tag_name2", "tag_value2")
    shape.add_tag(tag)
    shape.add_tag(tag2)
    print(shape.get_tags())


if __name__ == "__main__":
    main()
# method: set_file
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    model.set_file("model2.glb")
    print(model.get_file())


if __name__ == "__main__":
    main()
# method: get_file
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel("model.glb")
    print(model.get_file())


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_3dmodel("model.glb")
    print(shape.get_visibility())


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_3dmodel("model.glb")
    shape.set_visibility(False)
    print(shape.get_visibility())


if __name__ == "__main__":
    main()

Model3D.add_tag
ansa.report.Model3D.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Model3D.bring_forward
ansa.report.Model3D.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

Model3D.bring_to_front
ansa.report.Model3D.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

Model3D.get_file
ansa.report.Model3D.get_file() -> str
Returns the path of the underlying model file.
Returns: str

Model3D.get_position
ansa.report.Model3D.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x an y coordinates.

Model3D.get_report
ansa.report.Model3D.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

Model3D.get_rotation
ansa.report.Model3D.get_rotation() -> float
Return the angle of rotation of this shape in degrees.
Returns: float

Model3D.get_size
ansa.report.Model3D.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Model3D.get_slide
ansa.report.Model3D.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

Model3D.get_tags
ansa.report.Model3D.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Model3D.get_visibility
ansa.report.Model3D.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Model3D.remove_tag
ansa.report.Model3D.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Model3D.send_backward
ansa.report.Model3D.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

Model3D.send_to_back
ansa.report.Model3D.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

Model3D.set_file
ansa.report.Model3D.set_file(filename: str) -> bool
Sets the underlying model file.
- filename (str): Path to a .glb model file. Returns -------
Returns: bool

Model3D.set_name
ansa.report.Model3D.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

Model3D.set_position
ansa.report.Model3D.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Model3D.set_rotation
ansa.report.Model3D.set_rotation(angle: float) -> bool
Set the angle of rotation of this shape in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

Model3D.set_size
ansa.report.Model3D.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Model3D.set_visibility
ansa.report.Model3D.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

NamesOfAllShapes
ansa.report.NamesOfAllShapes(slide_name:str) -> dict
This function returns a map containing the names of the shapes in the given slide, grouped by shape type.
- slide_name (str): Name of the slide
Returns: It returns a dictionary which contains, the shape types as keys, and lists of shape names as values.
Upon failure, an empty dictionary is returned.

NamesOfAllSlides
ansa.report.NamesOfAllSlides() -> Iterable[str]
This function get names of all existing slides of the report.
Returns: It returns a list where each member of the list is a string referring to the name of an existing slide of the report.
Upon failure, an empty list is returned.
Example:
# PYTHON script
import ansa
from ansa import report

def main():
    all_slides = report.NamesOfAllSlides()
    for slide_name in all_slides:
        print(slide_name)

if __name__ == "__main__":
    main()


Picture
ansa.report.Picture(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A picture in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    # picture = slide.add_picture(filename, name = 'Image 1')
    # Second handler for the same picture using constructor
    picture2 = report.Picture(
        report_name=rep.name, slide_name=slide.name, name=picture.name
    )
    print(picture2)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    name = "picture name"
    ret = picture.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    rep = picture.get_report()
    if rep:
        print(rep)
        print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    slide = picture.get_slide()
    print(slide)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    position = picture.get_position()
    print(position)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    position = (0.5, 0.5)
    ret = picture.set_position(position)  # Middle of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    size = picture.get_size()
    print(size)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    size = (0.25, 0.25)
    ret = picture.set_size(size)  # A quarter of the size of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    ret = picture.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    ret = picture.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    ret = picture.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    ret = picture.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    angle = 90
    ret = picture.set_rotation(angle)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    rotation = picture.get_rotation()
    print(rotation)


if __name__ == "__main__":
    main()
# method: set_line_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    c = windows.Color(name="color1", r=0, g=255, b=0, a=255)
    ret = picture.set_line_style(
        fill="solid_line",
        color=c,
        width=1.0,
        dash="solid_line",
        custom_dash_pattern="111000",
        cap="flat_cap",
        join="bevel_join",
    )
    print(ret)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    c = windows.Color(name="color1", r=255, g=0, b=0, a=50)
    ret = picture.set_fill(fill="solid_fill", color=c, filename="image1.png")
    print(ret)


if __name__ == "__main__":
    main()
# method: set_image
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    filename = "image2.png"
    ret = picture.set_image("image2.png")
    print(ret)


if __name__ == "__main__":
    main()
# method: set_transparent_background
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    picture = slide.add_picture("image1.png")
    c = Color(name="color1", r=255, g=255, b=255, a=255)  # white
    transparent = True
    ret = picture.set_transparent_background(transparent, color=c)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_original_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    picture = slide.add_picture(filename)
    ret = picture.get_original_size()
    print(ret)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    shape = slide.add_picture(filename)
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    ret = shape.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = '"image1.png"'
    shape = slide.add_picture(filename)
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    ret = shape.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    shape = slide.add_picture(filename)
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    shape.add_tag(tag)
    shape.add_tag(tag2)
    tags = shape.get_tags()
    for tag in tags:
        print(tag)
        print(tag.name)
        print(tag.value)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    shape = slide.add_picture(filename)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "image1.png"
    shape = slide.add_picture(filename)
    visibility = False
    ret = shape.set_visibility(visibility)
    print(ret)


if __name__ == "__main__":
    main()

Picture.add_tag
ansa.report.Picture.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Picture.bring_forward
ansa.report.Picture.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

Picture.bring_to_front
ansa.report.Picture.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

Picture.get_original_size
ansa.report.Picture.get_original_size(unit: str) -> Iterable[float]
Returns the original width and height of the image in this shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Picture.get_position
ansa.report.Picture.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x an y coordinates.

Picture.get_report
ansa.report.Picture.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

Picture.get_rotation
ansa.report.Picture.get_rotation() -> float
Return the angle of rotation of this shape in degrees.
Returns: float

Picture.get_size
ansa.report.Picture.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Picture.get_slide
ansa.report.Picture.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

Picture.get_tags
ansa.report.Picture.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Picture.get_visibility
ansa.report.Picture.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Picture.remove_tag
ansa.report.Picture.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Picture.send_backward
ansa.report.Picture.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

Picture.send_to_back
ansa.report.Picture.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

Picture.set_fill
ansa.report.Picture.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str, optional): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

Picture.set_image
ansa.report.Picture.set_image(filename: str) -> bool
Set the displayed image.
- filename (str): Path to an image file. Returns -------
Returns: bool

Picture.set_line_style
ansa.report.Picture.set_line_style(fill: str, color: windows.Color, width: float, dash: str, custom_dash_pattern: str, cap: str, join: str) -> bool
Set various line style options. All arguments are optional, any number of them can be used. The result is visible only when fill is 'solid_line'.
- fill (str, optional): The available values are: - 'no_line' - 'solid_line'
- color (windows.Color, optional): meta.windows.Color
- width (float, optional): Line width.
- dash (str, optional): One of the following predefined dash patterns: 'solid_line' 'dash_line' 'dot_line' 'dash_dot_line' 'lg_dash_line' 'lg_dash_dot_line' 'lg_dash_dot_dot_line' 'sys_dash_line' 'sys_dash_dot_line' 'sys_dash_dot_dot_line' 'sys_dot_line' 'custom_dash_line'
- custom_dash_pattern (str, optional): A series of 1's and 0's describing a dash pattern. For example '111000'.
- cap (str, optional): One of the following: 'flat_cap' 'round_cap' 'square_cap'
- join (str, optional): One of the following: 'bevel_join' 'miter_join' 'round_join' Returns -------
Returns: bool

Picture.set_name
ansa.report.Picture.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

Picture.set_position
ansa.report.Picture.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Picture.set_rotation
ansa.report.Picture.set_rotation(angle: float) -> bool
Set the angle of rotation of this shape in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

Picture.set_size
ansa.report.Picture.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Picture.set_transparent_background
ansa.report.Picture.set_transparent_background(transparent: bool, color: windows.Color) -> bool
Toggle the transparent background option and select which color to make transparent.
- transparent (bool): 
- color (windows.Color, optional): meta.windows.Color Returns -------
Returns: bool

Picture.set_visibility
ansa.report.Picture.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

Rectangle
ansa.report.Rectangle(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A rectangle in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    print(rectangle)
    # Second handler for the same rectangle using constructor
    rectangle2 = report.Rectangle(
        report_name=rep.name, slide_name=slide.name, shape_name=rectangle.name
    )
    print(rectangle2)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    name = "rectangle name"
    ret = rectangle.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    rep = rectangle.get_report()
    print(rep)
    print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    slide = rectangle.get_slide()
    print(slide.name)
    print(slide.report_name)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    position = rectangle.get_position()
    print(position)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    position = (0.5, 0.5)
    ret = rectangle.set_position(position)  # Middle of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    size = rectangle.get_size()
    print(size)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    size = (0.25, 0.25)
    ret = rectangle.set_size(size)  # A quarter of the size of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    ret = rectangle.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    ret = rectangle.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    ret = rectangle.send_to_back()


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    ret = rectangle.send_backward()


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    angle = 90
    ret = rectangle.set_rotation(angle)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    rotation = rectangle.get_rotation()
    print(rotation)


if __name__ == "__main__":
    main()
# method: set_line_style
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    c = Color(name="color1", r=0, g=255, b=0, a=255)
    ret = rectangle.set_line_style(
        fill="solid_line",
        color=c,
        width=1.2,
        dash="solid_line",
        custom_dash_pattern="111000",
        cap="flat_cap",
        join="bevel_join",
    )
    print(ret)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "no_fill"
    # fill="solid_fill"
    # fill="picture_fill"
    ret = rectangle.set_fill(fill, color=c, filename="image.png")
    print(ret)


if __name__ == "__main__":
    main()
# method: set_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    size = (0.25, 0.25)
    ret = rectangle.set_size(size)
    text = "text"
    ret = rectangle.set_text(text)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    text = rectangle.get_text()
    print(text)


if __name__ == "__main__":
    main()
# method: set_text_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    ret = rectangle.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    # ret = rectangle.set_text_style(italic= True, underline= True, bold=True, strike_through=True, superscript = True, subscript = True)
    # color = windows.Color(r = 255, g = 255, b = 0, a = 255)
    # ret = rectangle.set_text_style(text_color = color,  text_align = 'left', line_spacing = 1.1, shape_align = 'top', autofit = 'no_autofit', wrap = True)
    print(ret)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_rectangle()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_rectangle()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)
    ret = shape.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_rectangle()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    shape.add_tag(tag)
    shape.add_tag(tag2)
    tags = shape.get_tags()
    for tag in tags:
        print(tag)
        print(tag.name, tag.value)


if __name__ == "__main__":
    main()
# method: get_text_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_rectangle()
    shape.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    # shape.set_text_style(italic=True, strike_through=True, superscript=True, subscript=True)
    # color = windows.Color(r = 255, g = 255, b = 0, a = 255)
    # shape.set_text_style(text_color = color, text_align = 'left', line_spacing = 1.2, space_align = 'top', autofit = 'no_autofit', wrap =True)
    style = shape.get_text_style()
    print(style)

    shape2 = slide.add_rectangle()
    shape2.set_text_style(**style)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_rectangle()
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_rectangle()
    shape.set_visibility(False)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()

Rectangle.add_tag
ansa.report.Rectangle.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Rectangle.bring_forward
ansa.report.Rectangle.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

Rectangle.bring_to_front
ansa.report.Rectangle.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

Rectangle.get_position
ansa.report.Rectangle.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

Rectangle.get_report
ansa.report.Rectangle.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

Rectangle.get_rotation
ansa.report.Rectangle.get_rotation() -> float
Return the angle of rotation of this shape in degrees.
Returns: float

Rectangle.get_size
ansa.report.Rectangle.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Rectangle.get_slide
ansa.report.Rectangle.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

Rectangle.get_tags
ansa.report.Rectangle.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Rectangle.get_text
ansa.report.Rectangle.get_text(html: bool) -> str
Returns the text of the shape.
- html (bool, optional): When True the text is returned in html formatting. The default value is False. Returns -------
Returns: str

Rectangle.get_text_style
ansa.report.Rectangle.get_text_style(position: int) -> dict
Returns the text style in the form of a python dictionary. The dictionary contains the same keys and can be applied directly to the set_text_style function. An optional position argument can be used to return the style of a specific character in the text.
- position (int, optional): Index of the character in the text whose style is returned. Indexing starts at 1. If undefined the style of the last character is returned. Returns -------
Returns: dict Python dictionary with the following text style options. font_family     string font_size       integer bold            boolean italic          boolean underline       boolean strike_through  boolean superscript     boolean subscript       boolean text_color      meta.windows.Color text_align      string    ('left', 'center', 'right', 'justify') shape_align     string    ('top', 'middle', 'bottom', 'top_center', 'middle_center', 'bottom_center') autofit         string    ('no_autofit', 'shrink_text_on_overflow', 'resize_shape_to_fit_text') wrap            boolean

Rectangle.get_visibility
ansa.report.Rectangle.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Rectangle.remove_tag
ansa.report.Rectangle.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Rectangle.send_backward
ansa.report.Rectangle.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

Rectangle.send_to_back
ansa.report.Rectangle.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

Rectangle.set_fill
ansa.report.Rectangle.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

Rectangle.set_line_style
ansa.report.Rectangle.set_line_style(fill: str, color: windows.Color, width: float, dash: str, custom_dash_pattern: str, cap: str, join: str) -> bool
Set various line style options. All arguments are optional, any number of them can be used. The result is visible only when fill is 'solid_line'.
- fill (str, optional): The available values are: - 'no_line' - 'solid_line'
- color (windows.Color, optional): meta.windows.Color
- width (float, optional): Line width.
- dash (str, optional): One of the following predefined dash patterns: 'solid_line' 'dash_line' 'dot_line' 'dash_dot_line' 'lg_dash_line' 'lg_dash_dot_line' 'lg_dash_dot_dot_line' 'sys_dash_line' 'sys_dash_dot_line' 'sys_dash_dot_dot_line' 'sys_dot_line' 'custom_dash_line'
- custom_dash_pattern (str, optional): A series of 1's and 0's describing a dash pattern. For example '111000'.
- cap (str, optional): One of the following: 'flat_cap' 'round_cap' 'square_cap'
- join (str, optional): One of the following: 'bevel_join' 'miter_join' 'round_join' Returns -------
Returns: bool

Rectangle.set_name
ansa.report.Rectangle.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

Rectangle.set_position
ansa.report.Rectangle.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Rectangle.set_rotation
ansa.report.Rectangle.set_rotation(angle: float) -> bool
Set the angle of rotation of this shape in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

Rectangle.set_size
ansa.report.Rectangle.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Rectangle.set_text
ansa.report.Rectangle.set_text(text: str) -> bool
Set the text. Accepts html for formatting.
- text (str): Returns -------
Returns: bool

Rectangle.set_text_style
ansa.report.Rectangle.set_text_style(font_family: str, font_size: int, bold: bool, italic: bool, underline: bool, strike_through: bool, superscript: bool, subscript: bool, text_color: windows.Color, text_align: str, shape_align: str, autofit: str, wrap: bool) -> bool
Set text style options. All arguments are optional. Any number of them can be used.
- font_family (str, optional): See corresponding setting in GUI for the available fonts.
- font_size (int, optional): See corresponding setting in GUI for size values.
- bold (bool, optional): 
- italic (bool, optional): 
- underline (bool, optional): 
- strike_through (bool, optional): 
- superscript (bool, optional): 
- subscript (bool, optional): 
- text_color (windows.Color, optional): meta.windows.Color
- text_align (str, optional): The following values are supported: 'left' 'center' 'right' 'justify'
- shape_align (str, optional): The following values are supported: 'top' 'middle' 'bottom' 'top_center' 'middle_center' 'bottom_center'
- autofit (str, optional): The following values are supported: 'no_autofit' 'shrink_text_on_overflow' 'resize_shape_to_fit_text'
- wrap (bool, optional): Wrap text option. Returns -------
Returns: bool

Rectangle.set_visibility
ansa.report.Rectangle.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

Report
ansa.report.Report(report_name: str, attached: bool = False) -> None
A report composer window in META. It can be used to create and edit reports.

Reports can be loaded from and exported to pptx files.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    # Second handler for the same report using constructor
    rep2 = report.Report(name=rep.name)
    print(rep2.name)


if __name__ == "__main__":
    main()
# method: open
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    filepath = "pres1.pptx"
    rep.open(filepath)


if __name__ == "__main__":
    main()
# method: save
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    filepath = "pres1.pptx"
    rep.save(filepath)


if __name__ == "__main__":
    main()
# method: reset
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    ret = rep.reset()
    print(ret)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = "Slide 1"
    slide = rep.get_slide(slide)
    if slide:
        print(slide)
        print(slide.name, slide.report_name)


if __name__ == "__main__":
    main()
# method: get_slides
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.add_slide()
    slide = rep.add_slide()
    slides = rep.get_slides()
    for s in slides:
        print(s)
        print(s.name, s.report_name)


if __name__ == "__main__":
    main()
# method: add_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.add_slide()
    slide_name = slide.name
    print(slide_name)


if __name__ == "__main__":
    main()
# method: move_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.add_slide()
    index = 2
    ret = rep.move_slide(slide, index)
    print(ret)


if __name__ == "__main__":
    main()
# method: duplicate_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.add_slide()
    dup_slide = rep.duplicate_slide(slide)


if __name__ == "__main__":
    main()
# method: delete_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.add_slide()
    ret = rep.delete_slide(slide)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_slide_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    size = (200, 100)
    ret = rep.set_slide_size(size)
    # ret = rep.set_slide_size(size, unit = 'mm')
    # ret = rep.set_slide_size(size, unit = 'mm', type = 'ao')
    # ret = rep.set_slide_size(size, unit = 'mm', type = 'ao', orientation = 'portrait')


if __name__ == "__main__":
    main()
# method: get_slide_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide_size = rep.get_slide_size()
    if slide_size:
        print("Width: " + str(slide_size[0]))
        print("Height: " + str(slide_size[1]))


if __name__ == "__main__":
    main()
# method: add_page
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    ret = rep.add_page(id=0)
    # ret = rep.add_page(name = 'Page Name')
    # ret = rep.add_page(title = 'Slide title')
    # ret = rep.add_page(add_title = True)
    # ret = rep.add_page(vectorized_image = True)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    rep.add_tag(tag)
    rep.add_tag(tag2)
    tags = rep.get_tags()
    for t in tags:
        print(t)
        print(t.name, t.value)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = rep.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    rep.add_tag(tag)
    rep.add_tag(tag2)
    ret = rep.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_attached
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    rep.set_attached(True)


if __name__ == "__main__":
    main()
# method: get_attached
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    print(rep.get_attached())


if __name__ == "__main__":
    main()

# method: set_current_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    rep.add_slide()
    rep.add_slide()
    rep.set_current_slide(2)


if __name__ == "__main__":
    main()
# method: get_current_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    print(rep.get_current_slide())


if __name__ == "__main__":
    main()

# method: set_visible
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    rep.set_visible(True)


if __name__ == "__main__":
    main()
# method: get_visible
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    print(rep.get_visible())


if __name__ == "__main__":
    main()

Report.add_page
ansa.report.Report.add_page(name: str, id: int, title: str, add_title: bool, vectorized_image: bool) -> bool
Creates a slide with the image of each window in the specified META page. The page is specified either by its name or its id.
- name (str, optional): Page name.
- id (int, optional): Page id.
- title (str, optional): Slide title text.
- add_title (bool, optional): Whether to add a title to the new slides or not. The default value is False.
- vectorized_image (bool, optional): Whether to drop the window image as a vectorized image. The default value is False. This option is valid only for 2D Plot windows. Returns -------
Returns: bool

Report.add_slide
ansa.report.Report.add_slide(name: str, index: int) -> Slide
Adds a new slide to the report. Optional name and index for the new slide can be provided. The new slide is returned.
- name (str, optional): Slide name.
- index (int, optional): Slide index. Slide numbering starts at 1. If not specified the new slide will be added at the end of the report. Returns -------
Returns: Slide meta.report.Slide

Report.add_tag
ansa.report.Report.add_tag(tag: ReportTag) -> bool
Adds a tag to the report.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Report.delete_slide
ansa.report.Report.delete_slide(slide: str | Slide) -> bool
Deletes the specified slide.
- slide (str | Slide): Either the name of the slide as a string or the meta.report.Slide object. Returns -------
Returns: bool

Report.duplicate_slide
ansa.report.Report.duplicate_slide(slide: Slide, name: str, index: int) -> Slide
Create a copy of the given slide. Optional name and index for the new slide can be provided. The new slide is returned.
- slide (Slide): meta.report.Slide to be duplicated.
- name (str, optional): New slide name
- index (int, optional): Index of the new slide. Returns -------
Returns: Slide meta.report.Slide

Report.get_attached
ansa.report.Report.get_attached() -> bool
Returns the attached status of the report window.
Returns: bool Whether the report window is attached or not.

Report.get_current_slide
ansa.report.Report.get_current_slide() -> Slide
Returns the current slide in the Report Composer.
Returns: Slide Current slide

Report.get_slide
ansa.report.Report.get_slide(slide: str | int) -> Slide
Returns the slide specified either by its name or its id.
- slide (str | int): Either the name of the slide as a string or the position of the slide as an integer. Slide numbering starts from 1. Returns -------
Returns: Slide meta.report.Slide

Report.get_slide_size
ansa.report.Report.get_slide_size(unit: str) -> list[float]
Returns the width and height of a slide.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'mm'. 'emu' 'cm' 'mm' 'inch' 'pt' 'pc' 'pixel' Returns -------
Returns: list[float] A list that contains the width and height in the requested units.

Report.get_slides
ansa.report.Report.get_slides() -> list[Slide]
Returns a list of the slides in this report.
Returns: list[Slide] List of meta.report.Slide objects.

Report.get_tags
ansa.report.Report.get_tags() -> list[ReportTag]
Returns the report tags.
Returns: list[ReportTag] List of report.ReportTag objects

Report.get_visible
ansa.report.Report.get_visible() -> bool
Return whether is window is shown or hidden.
Returns: bool True if window is visible, false if it is hidden

Report.move_slide
ansa.report.Report.move_slide(slide: Slide, index: int) -> bool
Move the given slide to the specified index.
- slide (Slide): meta.report.Slide
- index (int): New slide index. Numbering starts at 1. Returns -------
Returns: bool

Report.open
ansa.report.Report.open(filename: str, load_shape_names: bool = True, load_slide_names: bool = True) -> bool
Loads a report from the specified pptx file.
- filename (str): Path to pptx file.
- load_shape_names (bool, optional): Whether to load the shape names from the pptx file or not. Default value is True.
- load_slide_names (bool, optional): Whether to load the slide, layout, and master names from the pptx file or not. Default value is True. Returns -------
Returns: bool

Report.remove_tag
ansa.report.Report.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the report.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Report.reset
ansa.report.Report.reset() -> bool
Clears the report composer leaving only one empty slide.
Returns: bool

Report.save
ansa.report.Report.save(filename: str, options: dict) -> bool
Export the report to a pptx file.
- filename (str): Path to pptx file.
- options (dict, optional): Dictionary with string keys. Each key corresponds to an option. The available keys are listed below along with the type of value that is expected. 'redraw_excel_tables' : boolean 'excel_snapshots' : boolean 'vbscript_redraw' : boolean 'shrink_autofit' : boolean 'excel_2003_compatibility' : boolean 'excel_xls_compatibility' : boolean 'output_unparsed_text' : boolean 'compress_images_ppi' : integer (supported values: 220, 150, 96) Returns -------
Returns: bool

Report.set_attached
ansa.report.Report.set_attached(attached: bool) -> bool
Attach or detach the report window.
- attached (bool): True will make the window attached, false will make the window detached. Returns -------
Returns: bool

Report.set_current_slide
ansa.report.Report.set_current_slide(slide: int | str | Slide) -> bool
Sets the current slide in the Report Composer.
- slide (int | str | Slide): Either the index of the slide (1-indexed), the name of the slide, or the slide object that the user wishes to become current. Returns -------
Returns: bool True upon success. False upon failure to set the current slide, when the index is out of range or the slide name does not exist.

Report.set_slide_size
ansa.report.Report.set_slide_size(size: Iterable[float], unit: str, type: str, orientation: str) -> bool
Sets the slide width and height either using the 'size' argument or by selecting one of the predefined sizes with the combination of 'type' and 'orientation' arguments. If 'size' is used an optional unit can be provided.
- size (Iterable[float], optional): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'mm'. 'emu' 'cm' 'mm' 'inch' 'pt' 'pc' 'pixel'
- type (str, optional): When using this option an orientation must also be provided. Available values are: 'a0'  :  841 x 1189 mm 'a1'  :  594 x 841 mm 'a2'  :  420 x 594 mm 'a3'  :  297 x 420 mm 'a4'  :  210 x 297 mm, 8.26 x 11.69 inches 'a5'  :  148 x 210 mm 'a6'  :  105 x 148 mm 'a7'  :  74 x 105 mm 'a8'  :  52 x 74 mm 'a9'  :  37 x 52 mm 'b0'  :  1000 x 1414 mm 'b1'  :  707 x 1000 mm 'b2'  :  500 x 707 mm 'b3'  :  353 x 500 mm 'b4'  :  250 x 353 mm 'b5'  :  176 x 250 mm, 6.93 x 9.84 inches 'b6'  :  125 x 176 mm 'b7'  :  88 x 125 mm 'b8'  :  62 x 88 mm 'b9'  :  33 x 62 mm 'b10'  :  31 x 44 mm 'c5e'  :  163 x 229 mm 'comm10e'  :  105 x 241 mm, u.s. common 10 envelope 'dle'  :  110 x 220 mm 'executive'  :  190.5 x 254 mm, 7.5 x 10 inches, 'folio'  :  210 x 330 mm 'ledger'  :  431.8 x 279.4 mm 'legal'  :  215.9 x 355.6 mm, 8.5 x 14 inches, 'letter'  :  215.9 x 279.4 mm, 8.5 x 11 inches, 'tabloid'  :  279.4 x 431.8 mm 'film35mm'  :  36 x 24 mm 'screen16_9'  :  254 x 143.002 mm, 10 x 5.63 in 'screen16_10'  :  254 x 158.75 mm, 10 x 6.25 in 'screen4_3'  :       254 x 190.5 mm, 10 x 7.5 in
- orientation (str, optional): Either 'portrait' or 'landscape'. Returns -------
Returns: bool

Report.set_visible
ansa.report.Report.set_visible(visible: bool) -> bool
Shows or hides this window.
- visible (bool): Returns -------
Returns: bool

ReportFilter
ansa.report.ReportFilter(type: str, name: str, operator: str, value: str, value2: str) -> None
A ReportFilter object is used to find pptx files based on their tags. There are four possible types of ReportFilter objects (as specified by the first argument in the constructor): 'presentation', 'slide', 'element', and 'tag'.

ReportFilter objects of type 'tag' that specify a tag name, an operator such as 'equal', and a value represent specific rules by which to match tags. The other three types of ReportFilter objects, namely 'presentation', 'slide', and 'element' specify whether the search should match presentation tags, slide tags or element tags accordingly. This is achieved by adding the desired 'tag' object as a child of the corresponding 'presentation', 'slide', or 'element' object. 

The tree structure can be expanded to achieve more complex matching rules.

Constructor Arguments

type      string                Available values are listed below. Note that the 'tag' type also requires the rest of the arguments to be specified, a name, operator, and at least one value (depending on the operator).
                                 'presentation'
                                 'slide'
                                 'element'
                                 'tag'
name      string    (optional)  The tag name to search for.
operator  string    (optional)  Available values are listed below, note that some operators require a value2 argument as well.
                                 'equal'
                                 'less'
                                 'greater'
                                 'less_equal'
                                 'greater_equal'
                                 'not_equal'
                                 'between'
                                 'not_between'
                                 'contains'
                                 'not_contains'
                                 'starts_with'
                                 'not_starts_with'
                                 'ends_with'
                                 'not_ends_with'
                                 'reg_expression'
                                 'not_reg_expression'
value     string    (optional)  The tag value that is matched against the value of the tags in the report files using the specified operator.
value2    string    (optional)  A second optional value required by operators  'between' and 'not_between'.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    type = "presentation"
    # type = 'slide'
    # type = 'element'
    # type = 'tag'
    presFilter = report.ReportFilter(type)
    tagFilter = report.ReportFilter(
        type="tag", name="tag1", operator="equal", value="value1"
    )
    report_filter = presFilter.add_child(tagFilter)
    if report_filter:
        print(report_filter)


if __name__ == "__main__":
    main()
# method: search
# PYTHON script
import ansa
from ansa import report


def main():
    directory = "../"
    type = "presentation"
    # type = 'slide'
    # type = 'element'
    # type = 'tag'
    presFilter = report.ReportFilter(type)
    tagFilter = report.ReportFilter(
        type="tag", name="tag1", operator="equal", value="value1"
    )
    presFilter.add_child(tagFilter)
    print(presFilter.search(directory))


if __name__ == "__main__":
    main()
# method: get_children
# PYTHON script
import ansa
from ansa import report


def main():
    presFilter = report.ReportFilter(type="presentation")
    tagFilter = report.ReportFilter(
        type="tag", name="tag1", operator="equal", value="value1"
    )
    tagFilter2 = report.ReportFilter(
        type="tag", name="tag2", operator="equal", value="value2"
    )
    presFilter.add_child(tagFilter)
    presFilter.add_child(tagFilter2)
    print(presFilter.get_children())


if __name__ == "__main__":
    main()
# method: add_child
# PYTHON script
import ansa
from ansa import report


def main():
    presFilter = report.ReportFilter(type="presentation")
    tagFilter = report.ReportFilter(
        type="tag", name="tag1", operator="equal", value="value1"
    )
    tagFilter2 = report.ReportFilter(
        type="tag", name="tag2", operator="equal", value="value2"
    )
    presFilter.add_child(tagFilter)
    presFilter.add_child(tagFilter2)
    print(presFilter.get_children())


if __name__ == "__main__":
    main()
# method: remove_child
# PYTHON script
import ansa
from ansa import report


def main():
    presFilter = report.ReportFilter(type="presentation")
    tagFilter = report.ReportFilter(
        type="tag", name="tag1", operator="equal", value="value1"
    )
    tagFilter2 = report.ReportFilter(
        type="tag", name="tag2", operator="equal", value="value2"
    )
    presFilter.add_child(tagFilter)
    presFilter.add_child(tagFilter2)
    presFilter.remove_child(tagFilter)
    print(presFilter.get_children())


if __name__ == "__main__":
    main()

ReportFilter.add_child
ansa.report.ReportFilter.add_child(filter: ReportFilter) -> bool
Adds a child to this filter.
- filter (ReportFilter): report.ReportFilter object Returns -------
Returns: bool

ReportFilter.get_children
ansa.report.ReportFilter.get_children() -> Iterable[ReportFilter]
Returns a list with the children of this filter.
Returns: Iterable[ReportFilter] List of report.ReportFilter objects

ReportFilter.remove_child
ansa.report.ReportFilter.remove_child(filter: ReportFilter) -> bool
Removes the specified child.
- filter (ReportFilter): report.ReportFilter object Returns -------
Returns: bool

ReportFilter.search
ansa.report.ReportFilter.search(directory: str, folder_filter: str, file_filter: str, search_in_subfolders: bool) -> Iterable[str]
Finds pptx files whose tags match this filter, in the specified folder. Files can also be optionally filtered by their name using the file_filter option. Subfolders can be optionally search and filtered as well using the corresponding options.
- directory (str): Search directory.
- folder_filter (str, optional): A string to filter the sub-folders by their name. An '*' corresponds to any sub-string in the folder name. This argument is used only when search_in_subfolders is True. As an example a value of 'arp*' will match folders whose name begins with 'arp'.
- file_filter (str, optional): A string to filter the pptx files based on their name. An '*' corresponds to any sub-string in the file name. As an example a value of 'arp*' will match files whose name begins with 'arp'.
- search_in_subfolders (bool, optional): Whether to search sub-folders. Default value is False. Returns -------
Returns: Iterable[str] List of matching pptx file-paths

ReportTag
ansa.report.ReportTag(name: str, value: str) -> None
Represents a report tag. Tags can be added to presentations, slides, and slide elements. Tags have a tree structure. Each tag can contain other tags as children. Each tag is defined by its name and value.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    tag = report.ReportTag(name="tag_name", value="tag_value")
    print(tag)
    print(tag.name, tag.value)


if __name__ == "__main__":
    main()
# method: get_children
# PYTHON script
import ansa
from ansa import report


def main():
    tag = report.ReportTag(name="tag_name", value="tag_value")
    child1 = report.ReportTag(name="child_tag_name", value="child_tag_value")
    tag.add_child(child1)
    print(tag.get_children())


if __name__ == "__main__":
    main()
# method: add_child
# PYTHON script
import ansa
from ansa import report


def main():
    tag = report.ReportTag(name="tag_name", value="tag_value")
    child1 = report.ReportTag(name="child_tag_name", value="child_tag_value")
    tag.add_child(child1)
    print(tag.get_children())


if __name__ == "__main__":
    main()
# method: remove_child
# PYTHON script
import ansa
from ansa import report


def main():
    tag = report.ReportTag(name="tag_name", value="tag_value")
    child1 = report.ReportTag(name="tag_name2", value="tag_value2")
    child2 = report.ReportTag(name="tag_name3", value="tag_value3")
    tag.add_child(child1)
    tag.add_child(child2)
    tag.remove_child(child2)
    print(tag.get_children())


if __name__ == "__main__":
    main()

ReportTag.add_child
ansa.report.ReportTag.add_child(tag: ReportTag) -> bool
Adds a child to this tag.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

ReportTag.get_children
ansa.report.ReportTag.get_children() -> Iterable[ReportTag]
Returns a list with the children of this tag.
Returns: Iterable[ReportTag] List of report.ReportTag objects

ReportTag.remove_child
ansa.report.ReportTag.remove_child(tag: ReportTag | str) -> bool
Removes the specified child.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Reports
ansa.report.Reports() -> Iterable[Report]
Returns all report composer windows.
Returns: Returns a list of meta.report.Report objects that correspond to all report composer windows.
Example:
# PYTHON script
import os
import ansa
from ansa import report

def main():
    # Create new report
    r = report.CreateReport()
    print(r)
    # Or
    name = "Report 1"
    r = report.CreateReport(name)
    print(r)

    all_r = report.Reports()
    print(all_r)

if __name__ == "__main__":
    main()


ShapeGroup
ansa.report.ShapeGroup(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A shape group in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    # Second handler for the same group using constructor
    group2 = report.ShapeGroup(rep.name, slide.name, group.name)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    name = "group name"
    ret = group.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    rep = group.get_report()
    if rep:
        print(rep)
        print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    slide = group.get_slide()
    print(slide)
    print(slide.name)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    position = group.get_position()
    print(position)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    position = (0.5, 0.5)
    ret = group.set_position(position)  # Middle of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    size = group.get_size()
    print(size)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    size = (0.25, 0.25)
    ret = group.set_size(size)  # A quarter of the size of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    ret = group.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    ret = group.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    ret = group.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    ret = group.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    angle = 90
    ret = group.set_rotation(angle)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    position = group.get_rotation()
    print(position)


if __name__ == "__main__":
    main()
# method: set_line_style
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    c = Color(name="color1", r=0, g=255, b=0, a=255)
    ret = group.set_line_style(fill="solid_line", color=c)
    ret = group.set_line_style(
        width=1.1,
        dash="solid_line",
        custom_dash_pattern="111000",
        cap="flat_cap",
        join="bevel_join",
    )
    print(ret)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "no_fill"
    # fill="solid_fill"
    # fill="picture_fill"
    ret = group.set_fill(fill, color=c)
    print(ret)


if __name__ == "__main__":
    main()
# method: ungroup
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    ret = group.ungroup()
    print(ret)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    shape = slide.add_group([rect1, rect2, rect3])
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    shape.add_tag(tag)
    tags = shape.get_tags()
    for tag in tags:
        print(tag.name)
        print(tag.value)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    shape = slide.add_group([rect1, rect2, rect3])
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)
    ret = shape.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    shape = slide.add_group([rect1, rect2, rect3])
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)
    ret = shape.add_tag(tag2)
    print(ret)
    tags = shape.get_tags()
    for tag in tags:
        print(tag.name, tag.value)


if __name__ == "__main__":
    main()
# method: get_shapes
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    shapes = group.get_shapes()
    for shape in shapes:
        print(shape)
        print(shape.name)
        print(shape.slide_name)
        print(shape.report_name)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    ret = group.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rect1 = slide.add_rectangle()
    rect2 = slide.add_rectangle()
    rect3 = slide.add_rectangle()
    group = slide.add_group([rect1, rect2, rect3])
    group.set_visibility(False)
    ret = group.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()

ShapeGroup.add_tag
ansa.report.ShapeGroup.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

ShapeGroup.bring_forward
ansa.report.ShapeGroup.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

ShapeGroup.bring_to_front
ansa.report.ShapeGroup.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

ShapeGroup.get_position
ansa.report.ShapeGroup.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x an y coordinates.

ShapeGroup.get_report
ansa.report.ShapeGroup.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

ShapeGroup.get_rotation
ansa.report.ShapeGroup.get_rotation() -> float
Return the angle of rotation of this shape in degrees.
Returns: float

ShapeGroup.get_shapes
ansa.report.ShapeGroup.get_shapes() -> list
Returns the shapes (textboxes, tables, images, lines, etc) that belong to this group.
Returns: list List of meta.report.Rectangle, meta.report.Triangle, meta.report.Line, meta.report.ShapeGroup, meta.report.Textbox, meta.report.Table etc. objects.

ShapeGroup.get_size
ansa.report.ShapeGroup.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

ShapeGroup.get_slide
ansa.report.ShapeGroup.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

ShapeGroup.get_tags
ansa.report.ShapeGroup.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

ShapeGroup.get_visibility
ansa.report.ShapeGroup.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

ShapeGroup.remove_tag
ansa.report.ShapeGroup.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

ShapeGroup.send_backward
ansa.report.ShapeGroup.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

ShapeGroup.send_to_back
ansa.report.ShapeGroup.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

ShapeGroup.set_fill
ansa.report.ShapeGroup.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Filename pointing to an image file. Returns -------
Returns: bool

ShapeGroup.set_line_style
ansa.report.ShapeGroup.set_line_style(fill: str, color: windows.Color, width: float, dash: str, custom_dash_pattern: str, cap: str, join: str) -> bool
Set various line style options. All arguments are optional, any number of them can be used. The result is visible only when fill is 'solid_line'.
- fill (str, optional): The available values are: - 'no_line' - 'solid_line'
- color (windows.Color, optional): meta.windows.Color
- width (float, optional): Line width.
- dash (str, optional): One of the following predefined dash patterns: 'solid_line' 'dash_line' 'dot_line' 'dash_dot_line' 'lg_dash_line' 'lg_dash_dot_line' 'lg_dash_dot_dot_line' 'sys_dash_line' 'sys_dash_dot_line' 'sys_dash_dot_dot_line' 'sys_dot_line' 'custom_dash_line'
- custom_dash_pattern (str, optional): A series of 1's and 0's describing a dash pattern. For example '111000'.
- cap (str, optional): One of the following: 'flat_cap' 'round_cap' 'square_cap'
- join (str, optional): One of the following: 'bevel_join' 'miter_join' 'round_join' Returns -------
Returns: bool

ShapeGroup.set_name
ansa.report.ShapeGroup.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

ShapeGroup.set_position
ansa.report.ShapeGroup.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

ShapeGroup.set_rotation
ansa.report.ShapeGroup.set_rotation(angle: float) -> bool
Set the angle of rotation of this shape in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

ShapeGroup.set_size
ansa.report.ShapeGroup.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

ShapeGroup.set_visibility
ansa.report.ShapeGroup.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

ShapeGroup.ungroup
ansa.report.ShapeGroup.ungroup() -> bool
Resolves the group into the shapes it consists of. Invalidates this object.
Returns: bool

Slide
ansa.report.Slide(report_name: str, slide_name: str, attached: bool = False) -> None
A slide in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    # Second handler for the same slide using constructor
    slide2 = report.Slide(report_name=rep.name, slide_name=slide.name)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    name = "slide name"
    ret = slide.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    r = slide.get_report()
    print(r, r.name)


if __name__ == "__main__":
    main()
# method: get_shape
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    textbox = slide.add_textbox()
    print(line)
    print(line.name, line.slide_name, line.report_name)

    # Second handler for line
    name = line.name
    line2 = slide.get_shape(name)
    print(line2)
    print(line2.name, line2.slide_name, line2.report_name)


if __name__ == "__main__":
    main()
# method: get_shapes
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ret = slide.add_line()
    print(ret)
    ret = slide.add_textbox()
    print(ret)
    shapes = slide.get_shapes()
    for s in shapes:
        print(s)


if __name__ == "__main__":
    main()
# method: add_group
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    textbox = slide.add_textbox()
    group = slide.add_group([line, textbox])
    print(group)
    print(group.name)


if __name__ == "__main__":
    main()
# method: add_excel_table
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_excel_table()
    # filename = './example.xlsx'
    # name = 'MyShape'
    # table = slide.add_excel_table(filename, name)


if __name__ == "__main__":
    main()
# method: add_textbox
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    textbox = slide.add_textbox()
    position = (0.3, 0.3)
    textbox.set_position(position)
    text = "some text"
    textbox.set_text(text)


if __name__ == "__main__":
    main()
# method: delete_shapes
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    textbox = slide.add_textbox()
    ret = slide.delete_shapes([line])
    print(ret)


if __name__ == "__main__":
    main()
# method: add_picture
# PYTHON script
import ansa
from ansa import report


def main():
    path = "example/path/to/image.png"
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    picture = slide.add_picture(path)
    print(picture)


if __name__ == "__main__":
    main()
# method: add_table
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 5
    table = slide.add_table(rows, columns)
    print(table)
    print(table.name)


if __name__ == "__main__":
    main()
# method: add_line
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    line = slide.add_line()
    position = (0.2, 0.5)
    line.set_p1_position(position)
    position = (0.7, 0.5)
    unit = "norm"
    unit = "emu"
    unit = "cm"
    unit = "mm"
    unit = "inch"
    unit = "pt"
    unit = "pc"
    unit = "pixel"
    unit = "norm"
    line.set_p2_position(position)


if __name__ == "__main__":
    main()
# method: add_rectangle
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rectangle = slide.add_rectangle()
    size = (0.4, 0.3)
    ret = rectangle.set_size(size)
    print(ret)
    position = (0.3, 0.3)
    ret = rectangle.set_position(position)
    print(ret)


if __name__ == "__main__":
    main()
# method: add_ellipse
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ellipse = slide.add_ellipse()
    size = (0.4, 0.3)
    ret = ellipse.set_size(size)
    print(ret)
    position = (0.3, 0.3)
    ret = ellipse.set_position(position)
    print(ret)


if __name__ == "__main__":
    main()
# method: add_triangle
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    size = (0.3, 0.3)
    triangle.set_size(size)
    position = (0.3, 0.3)
    triangle.set_position(position)
    # unit = 'norm'
    # unit = 'emu'
    # unit = 'cm'
    # unit = 'mm'
    # unit = 'inch'
    # unit = 'pt'
    # unit = 'pc'
    # unit = 'pixel'
    # triangle.set_position(position, unit)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    visibility = slide.set_visibility(False)
    print(visibility)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    ret = slide.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_layout
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    master = "Default Slide Master"
    layout = "Layout 1"
    ret = slide.set_layout(master, layout)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_layout_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    layout_name = slide.get_layout_name()
    print(layout_name)


if __name__ == "__main__":
    main()
# method: get_master_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    master_name = slide.get_master_name()
    print(master_name)


if __name__ == "__main__":
    main()
# method: set_background
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "no_fill"
    slide.set_background(fill, color=c)
    # fill="solid_fill"
    # color=c
    # slide.set_background(fill="solid_fill", color=c)
    # fill="picture_fill"
    # filename = './image.png'
    # slide.set_background(fill, filename)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = slide.add_tag(tag)
    print(ret)
    ret = slide.get_tags()
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    slide.add_tag(tag)
    slide.add_tag(tag2)
    slide.remove_tag(tag)
    tag = slide.get_tags()
    print(tag, tag.name)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag("tag_name2", "tag_value2")
    slide.add_tag(tag)
    slide.add_tag(tag2)
    tags = slide.get_tags()
    for tag in tags:
        print(tag, tag.name)


if __name__ == "__main__":
    main()
# method: add_freeform
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    freeform = slide.add_freeform()
    points = [(0.4, 0.4), (0.2, 0.6), (0.5, 0.6)]
    freeform.set_points(points)
    # unit ='emu'
    # unit = 'cm'
    # unit = 'mm'
    # unit = 'inch'
    # unit = 'pt'
    # unit = 'pc'
    # unit = 'pixel'
    # unit = 'norm'
    # freeform.set_points(points, unit)


if __name__ == "__main__":
    main()
# method: add_3dmodel
# PYTHON script
import ansa
from ansa import report


def main():
    path = "example/path/to/model.glb"
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    model = slide.add_3dmodel(path)
    print(model)


if __name__ == "__main__":
    main()
# method: add_video
# PYTHON script
import ansa
from ansa import report


def main():
    path = "example/path/to/video"
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_video(path)
    print(shape)


if __name__ == "__main__":
    main()
# method: add_audio
# PYTHON script
import ansa
from ansa import report


def main():
    path = "example/path/to/audio"
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_audio(path)
    print(shape)


if __name__ == "__main__":
    main()

Slide.add_3dmodel
ansa.report.Slide.add_3dmodel(filename: str, name: str) -> Model3D
Creates a new 3D model shape in this slide. The new shape is returned.
- filename (str): Path to a .glb model file.
- name (str, optional): Shape name. Returns -------
Returns: Model3D meta.report.Model3D

Slide.add_audio
ansa.report.Slide.add_audio(filename: str, name: str, embed: bool) -> Audio
Creates a new audio shape in this slide. The new shape is returned.
- filename (str): Path to an audio file.
- name (str, optional): Shape name.
- embed (bool, optional): Whether the file will be embedded or linked on export. The default value is True. Returns -------
Returns: Audio meta.report.Audio

Slide.add_ellipse
ansa.report.Slide.add_ellipse(name: str) -> Ellipse
Create a new ellipse in the slide. The new ellipse is returned.
- name (str, optional): Shape name Returns -------
Returns: Ellipse meta.report.Ellipse

Slide.add_excel_table
ansa.report.Slide.add_excel_table(filename: str, name: str) -> ExcelTable
Creates a new excel table in the slide. The new table is returned.
- filename (str, optional): Path to an .xlsx file.
- name (str, optional): Shape name. Returns -------
Returns: ExcelTable meta.report.ExcelTable

Slide.add_freeform
ansa.report.Slide.add_freeform(points: list, unit: str, name: str) -> Freeform
Creates a new freeform shape in the slide. The new shape is returned.
- points (list): A non-empty list of pairs of x, y coordinates.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions
- name (str, optional): Shape name. Returns -------
Returns: Freeform meta.report.Freeform

Slide.add_group
ansa.report.Slide.add_group(shapes: list, name: str) -> ShapeGroup
Create a new group with the given shapes. The new group is returned.
- shapes (list): A list of shape objects such as meta.report.Rectangle, meta.report.Textbox, meta.report.Table etc.
- name (str, optional): Shape name. Returns -------
Returns: ShapeGroup meta.report.ShapeGroup

Slide.add_line
ansa.report.Slide.add_line(name: str) -> Line
Create a new line in the slide. The new line is returned.
- name (str, optional): Shape name. Returns -------
Returns: Line meta.report.Line

Slide.add_picture
ansa.report.Slide.add_picture(filename: str, name: str) -> Picture
Creates a new picture in this slide. The new picture is returned.
- filename (str): Path to an image file.
- name (str, optional): Shape name. Returns -------
Returns: Picture meta.report.Picture

Slide.add_rectangle
ansa.report.Slide.add_rectangle(name: str) -> Rectangle
Creates a new rectangle in the slide. The new recctangle is returned.
- name (str, optional): Shape name. Returns -------
Returns: Rectangle meta.report.Rectangle

Slide.add_table
ansa.report.Slide.add_table(rows: int, columns: int, name: str) -> Table
Creates a new table in this slide. The new table is returned.
- rows (int): Number of rows.
- columns (int): Number of columns.
- name (str, optional): Shape name. Returns -------
Returns: Table meta.report.Table

Slide.add_tag
ansa.report.Slide.add_tag(tag: ReportTag) -> bool
Adds a tag to the slide.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Slide.add_textbox
ansa.report.Slide.add_textbox(text: str, name: str) -> Textbox
Create a new textbox in this slide. The new textbox is returned.
- text (str): The text to be displayed in the textbox. It may be plain text or HTML formatted.
- name (str, optional): Shape name. Returns -------
Returns: Textbox meta.report.Textbox

Slide.add_triangle
ansa.report.Slide.add_triangle(name: str) -> Triangle
Create a new triangle in the slide. The new triangle is returned.
- name (str, optional): Shape name. Returns -------
Returns: Triangle meta.report.Triangle

Slide.add_video
ansa.report.Slide.add_video(filename: str, name: str, embed: bool) -> VideoShape
Creates a new video shape in this slide. The new shape is returned.
- filename (str): Path to a video file.
- name (str, optional): Shape name.
- embed (bool, optional): Whether the file will be embedded or linked on export. The default value is True. Returns -------
Returns: VideoShape meta.report.VideoShape

Slide.delete_shapes
ansa.report.Slide.delete_shapes(shapes: list) -> bool
Deletes the specified shapes from the slide.
- shapes (list): Either the name of a shape as a string, a list of shape names, a shape object, or a list of shape objects. Returns -------
Returns: bool

Slide.get_layout_name
ansa.report.Slide.get_layout_name() -> str
Returns layout name of this slide.
Returns: str Layout name.

Slide.get_master_name
ansa.report.Slide.get_master_name() -> str
Returns the master name of this slide.
Returns: str Master layout name.

Slide.get_report
ansa.report.Slide.get_report() -> Report
Return the report that this slide belongs to.
Returns: Report meta.report.Report

Slide.get_shape
ansa.report.Slide.get_shape(name: str) -> list
Returns the shape with the specified name.
- name (str): Shape name. Returns -------
Returns: list Shape object such as meta.report.Textbox, meta.report.Table etc.

Slide.get_shapes
ansa.report.Slide.get_shapes() -> list
Returns a list of the shapes (textboxes, tables, images, lines, etc) in this slide.
Returns: list List of meta.report.Rectangle, meta.report.Triangle, meta.report.Line, meta.report.ShapeGroup, meta.report.Textbox, meta.report.Table etc. objects.

Slide.get_tags
ansa.report.Slide.get_tags() -> Iterable[ReportTag]
Returns the slide tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Slide.get_visibility
ansa.report.Slide.get_visibility() -> bool
Returns the visibility of the slide.
Returns: bool Visibility of the slide.

Slide.remove_tag
ansa.report.Slide.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the slide.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Slide.set_background
ansa.report.Slide.set_background(fill: str, color: windows.Color, filename: str) -> bool
Sets the slide background. Either to a solid color or an image according to the specifed arguments.
- fill (str): Supported values are: 'no_fill' 'solid_fil' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color. Used when the fill option is 'solid_fill'.
- filename (str, optional): Path to an image file. Used when the fill option is 'picture_fill'. Returns -------
Returns: bool

Slide.set_layout
ansa.report.Slide.set_layout(master: str, layout: str) -> bool
Sets the layout for this slide.
- master (str): Master name.
- layout (str): Layout name. Returns -------
Returns: bool

Slide.set_name
ansa.report.Slide.set_name(name: str) -> bool
Sets the slide name.
- name (str): Slide name. Returns -------
Returns: bool

Slide.set_visibility
ansa.report.Slide.set_visibility(visibility: bool) -> bool
Sets the visibility of the slide.
- visibility (bool): Returns -------
Returns: bool

Table
ansa.report.Table(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A table in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    # Second handler for the same textbox using constructor
    table2 = report.Table(
        report_name=rep.name, slide_name=slide.name, shape_name=table.name
    )
    print(table2)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    name = "table name"
    ret = table.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    rep = table.get_report()
    if rep:
        print(rep)
        print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    slide = table.get_slide()
    if slide:
        print(slide)
        print(slide.name)
        print(slide.report_name)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = table.get_position()
    print(position)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = (0.5, 0.5)
    ret = table.set_position(position)  # Middle of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    size = table.get_size()
    print(size)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    size = (0.25, 0.25)
    ret = table.set_size(size)  # A quarter of the size of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    ret = table.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    ret = table.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    ret = table.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    ret = table.send_backward()


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_table(3, 2)
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "solid_fill"
    ret = table.set_fill(fill, color=c)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    text = "text"
    ret = table.set_text(text)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_text_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    color = windows.Color(r=255, g=255, b=0, a=255)
    ret = table.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    # ret = table.set_text_style(italic=True, underline= True, strike_through=True, superscript=True, subscript = True)
    # ret = table.set_text_style(text_color = color, text_align = 'left', line_spacing = 1.2, shape_align = 'top', autofit = 'no_autofit', wrap = True)


if __name__ == "__main__":
    main()
# method: get_cell
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = (1, 2)
    cell = table.get_cell(position)
    print(cell.shape_name)
    print(cell.slide_name)
    print(cell.report_name)
    print(cell.row)
    print(cell.column)


if __name__ == "__main__":
    main()
# method: get_range
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)


if __name__ == "__main__":
    main()
# method: get_row
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    row = 2
    range = table.get_row(row)
    if range:
        print(range)
        print(range.shape_name)
        print(range.slide_name)
        print(range.report_name)
        print(range.min_row)
        print(range.min_column)
        print(range.max_row)
        print(range.max_column)


if __name__ == "__main__":
    main()
# method: get_column
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    column = 2
    range = table.get_column(column)
    if range:
        print(range)
        print(range.shape_name)
        print(range.slide_name)
        print(range.report_name)
        print(range.min_row)
        print(range.min_column)
        print(range.max_row)
        print(range.max_column)


if __name__ == "__main__":
    main()
# method: set_row_height
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    height = 0.3
    ret = table.set_row_height(height)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_column_width
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    width = 0.2
    ret = table.set_column_width(width)
    # u = 'emu'
    # u = 'cm'
    # u = 'mm'
    # u = 'inch'
    # u = 'pt'
    # u = 'pc'
    # u = 'pixel'
    # u = 'norm'
    # ret = table.set_column_width(width, unit = u)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_table_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    rows = 4
    columns = 4
    ret = table.set_table_size(rows, columns)
    print(ret)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_table(3, 2)
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_table(3, 2)
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    shape.add_tag(tag)
    ret = shape.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_table(3, 2)
    tag = report.CreateReportTag("tag_name", "tag_value")
    tag2 = report.CreateReportTag("tag_name2", "tag_value2")
    shape.add_tag(tag)
    shape.add_tag(tag2)
    tags = shape.get_tags()
    for tag in tags:
        print(tag)
        print(tag.name)
        print(tag.value)


if __name__ == "__main__":
    main()
# method: get_table_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    rows = 4
    columns = 3
    ret = table.set_table_size(rows, columns)
    print(ret.get_table_size())


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    ret = table.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    visibility = False
    ret = table.set_visibility(visibility)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_margins
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    text = "text"
    ret = table.set_text(text)
    print(ret)

    ret = table.set_margins(left=2, top=2)
    u = "cm"
    u = "mm"
    u = "inch"
    u = "pt"
    u = "pixel"
    ret = table.set_margins(right=2, bottom=2, unit=u)


if __name__ == "__main__":
    main()

Table.add_tag
ansa.report.Table.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Table.bring_forward
ansa.report.Table.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

Table.bring_to_front
ansa.report.Table.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

Table.get_cell
ansa.report.Table.get_cell(position: Iterable[int]) -> TableCell
Return a specific cell of this table.
- position (Iterable[int]): A list or 2-tuple with the row and column of the desired cell. Index starts at 1. Returns -------
Returns: TableCell meta.report.TableCell

Table.get_column
ansa.report.Table.get_column(column: int) -> TableRange
Returns a range that represents the desired column on this table.
- column (int): The desired column. Index starts at 1. Returns -------
Returns: TableRange meta.report.TableRange

Table.get_position
ansa.report.Table.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

Table.get_range
ansa.report.Table.get_range(top_left: Iterable[int], bottom_right: Iterable[int]) -> TableRange
Return a specific range on this table. The range is a rectangle of cells and is specified by the row and column of the top left and bottom right corners.
- top_left (Iterable[int]): A list or 2-tuple with the row and column of the top left corner of the desired range. Index starts at 1.
- bottom_right (Iterable[int]): A list or 2-tuple with the row and column of the bottom right corner of the desired range. Index starts at 1. Returns -------
Returns: TableRange meta.report.TableRange

Table.get_report
ansa.report.Table.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

Table.get_row
ansa.report.Table.get_row(row: int) -> TableRange
Returns a range that represents the desired row on this table.
- row (int): The desired row. Index starts at 1. Returns -------
Returns: TableRange meta.report.TableRange

Table.get_size
ansa.report.Table.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Table.get_slide
ansa.report.Table.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

Table.get_table_size
ansa.report.Table.get_table_size() -> Iterable[int]
Returns the number of rows and columns in the table as a list.
Returns: Iterable[int] A list where the first element is the number of rows and the second the number of columns.

Table.get_tags
ansa.report.Table.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Table.get_visibility
ansa.report.Table.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Table.remove_tag
ansa.report.Table.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Table.send_backward
ansa.report.Table.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

Table.send_to_back
ansa.report.Table.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

Table.set_column_width
ansa.report.Table.set_column_width(width: float, unit: str) -> bool
Set the column width. Takes an optional unit argument.
- width (float): Column width.
- unit (str, optional): Unit of the given width. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Table.set_fill
ansa.report.Table.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

Table.set_margins
ansa.report.Table.set_margins(left: float, right: float, top: float, bottom: float, unit: str) -> bool
Sets the margin between the text and the edges of each cell, for all the cells in the table.
- left (float, optional): Left margin.
- right (float, optional): Right margin.
- top (float, optional): Top margin.
- bottom (float, optional): Bottom margin.
- unit (str, optional): Unit of the given margins. Available values are listed below. The default value is 'mm'. - 'cm' - 'mm' - 'inch' - 'pt' - 'pixel' Returns -------
Returns: bool

Table.set_name
ansa.report.Table.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

Table.set_position
ansa.report.Table.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Table.set_row_height
ansa.report.Table.set_row_height(height: float, unit: str) -> bool
Set the row height. Takes an optional unit argument.
- height (float): Row height.
- unit (str, optional): Unit of the given height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Table.set_size
ansa.report.Table.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Table.set_table_size
ansa.report.Table.set_table_size(rows: int, columns: int) -> bool
Set the number of rows and columns of the table.
- rows (int): Number of rows.
- columns (int): Number of columns. Returns -------
Returns: bool

Table.set_text
ansa.report.Table.set_text(text: str) -> bool
Set the text of every cell in the table. Accepts html for formatting.
- text (str): Returns -------
Returns: bool

Table.set_text_style
ansa.report.Table.set_text_style(font_family: str, font_size: int, bold: bool, italic: bool, underline: bool, strike_through: bool, superscript: bool, subscript: bool, text_color: windows.Color, text_align: str, shape_align: str) -> bool
Set text style options. All arguments are optional. Any number of them can be used.
- font_family (str, optional): See corresponding setting in GUI for the available fonts.
- font_size (int, optional): See corresponding setting in GUI for size values.
- bold (bool, optional): 
- italic (bool, optional): 
- underline (bool, optional): 
- strike_through (bool, optional): 
- superscript (bool, optional): 
- subscript (bool, optional): 
- text_color (windows.Color, optional): meta.windows.Color
- text_align (str, optional): The following values are supported: 'left' 'center' 'right' 'justify'
- shape_align (str, optional): The following values are supported: 'top' 'middle' 'bottom' 'top_center' 'middle_center' 'bottom_center' Returns -------
Returns: bool

Table.set_visibility
ansa.report.Table.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

TableCell
ansa.report.TableCell(report_name: str, slide_name: str, shape_name: str, position: list, attached: bool = False) -> None
A cell on a table in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    # Second handler for the same textbox using constructor
    cell2 = report.TableCell(rep.name, slide.name, table.name, [1, 1])
    print(cell2)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    rep = cell.get_report()
    print(rep)
    print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell([1, 1])
    slide = cell.get_slide()
    print(slide)
    print(slide.name)
    print(slide.report_name)


if __name__ == "__main__":
    main()
# method: get_shape
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    print(cell.get_shape())
    print(cell.slide_name, cell.report_name, cell.row, cell.column)


if __name__ == "__main__":
    main()
# method: set_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    text = "text"
    ret = cell.set_text(text)
    print(text)


if __name__ == "__main__":
    main()
# method: get_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    text = "some text"
    ret = cell.set_text(text)
    text = cell.get_text()
    print(text)


if __name__ == "__main__":
    main()
# method: set_text_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    table = slide.add_table(3, 2)
    cell = table.get_cell([1, 1])
    ret = cell.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    # ret = cell.set_text_style(italic=True, underline=True, strike_through=True, superscript=True, subscript =True)
    # ret = cell.set_text_style(italic=True, underline=True, strike_through=True, superscript=True, subscript =True)
    # color = windows.Color(r = 255, g = 255, b = 0, a = 255)
    # ret = cell.set_text_style(text_color = color, text_align = 'left', shape_align = 'top')
    print(ret)


if __name__ == "__main__":
    main()
# method: set_row_height
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    height = 0.3
    ret = cell.set_row_height(height)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_column_width
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    width = 0.3
    ret = cell.set_column_width(width)
    # u = 'emu'
    # ret = cell.set_column_width(width, unit = u)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "no_fill"
    ret = cell.set_fill(fill)
    # ret = cell.set_fill(fill, color=c)
    # ret = cell.set_fill(fill, filaname = '/home/image.png')
    print(ret)


if __name__ == "__main__":
    main()
# method: get_text_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    ret = cell.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    # ret = cell.set_text_style(italic=True, underline=True, bold=True, strike_through=True, superscript =True, subscript =True)
    # color = windows.Color(r = 255, g = 255, b = 0, a = 255)
    # ret = cell.set_text_style(text_color = color, text_align = 'left', shape_align = 'top')
    style = cell.get_text_style()
    print(style)
    position = [1, 2]
    cell2 = table.get_cell(position)
    ret = cell2.set_text_style(**style)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_margins
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    text = "text"
    ret = cell.set_text(text)
    print(ret)
    ret = cell.set_margins(left=2, top=2)
    ret = cell.set_margins(right=2, bottom=2)
    ret = cell.set_margins(right=2, bottom=2, unit="cm")
    print(ret)
    u = "cm"
    margins = cell.get_margins(unit=u)
    print(margins)


if __name__ == "__main__":
    main()
# method: get_margins
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    text = "text"
    ret = cell.set_text(text)
    left = 2
    top = 2
    ret = cell.set_margins(left, top)
    # ret = cell.set_margins(right, bottom)
    # u = 'cm'
    # ret = cell.set_margins(right, bottom, unit = u)
    margins = cell.get_margins()
    # margins = cell.get_margins(unit = u)
    print(margins)


if __name__ == "__main__":
    main()
# method: set_border_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)

    cell1 = table.get_cell((1, 2))
    cell2 = table.get_cell((2, 2))
    color = windows.Color(name="color1", r=0, g=255, b=0, a=255)

    cell1.set_border_style("left", "no_line", 2, color)
    cell2.set_border_style("left", "dash_line", 2, color)


if __name__ == "__main__":
    main()
# method: get_border_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)

    cell = table.get_cell((1, 2))
    borderStyle = cell.get_border_style("left")
    print(borderStyle)


if __name__ == "__main__":
    main()

TableCell.get_border_style
ansa.report.TableCell.get_border_style(side: str) -> dict
Returns the border style for the specified side of this cell. The returned dictionary can be directly applied to method set_border_style using the argument expand (**) operator.
- side (str): One of the following values: 'left', 'right', 'top', or 'bottom' Returns -------
Returns: dict Dictionary keys: side: 'left', 'right', 'top', or 'bottom' width: float color: windows.Color object dash: predefined dash pattern string

TableCell.get_fill
ansa.report.TableCell.get_fill() -> dict
Returns a dictionary describing the cell fill.
Returns: dict A dictionary with the following keys/values: 'fill': Either 'no_fill', solid_fill, or 'picture_fill' 'color': A Color object, valid when 'fill' is 'solid_fill' 'filename': The picture filename, valid when 'fill' is 'picture_fill' Examples --------
Example:
# method: get_fill
# PYTHON script
import meta
from meta import report
from meta.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    position = [1, 1]
    cell = table.get_cell(position)
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "solid_fill"
    ret = cell.set_fill(fill, c)
    print(cell.get_fill())


if __name__ == "__main__":
    main()

TableCell.get_margins
ansa.report.TableCell.get_margins(unit: str) -> Iterable[float]
Returns the margin between the text and the edges of the cell.
- unit (str, optional): Unit of the returned margins. Available values are listed below. The default value is 'mm'. - 'cm' - 'mm' - 'inch' - 'pt' - 'pixel' Returns -------
Returns: Iterable[float] Array of floats with the respective margin [left, right, top, bottom]

TableCell.get_report
ansa.report.TableCell.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

TableCell.get_shape
ansa.report.TableCell.get_shape() -> Table
Return the table that this cell belongs to.
Returns: Table meta.report.Table

TableCell.get_slide
ansa.report.TableCell.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

TableCell.get_text
ansa.report.TableCell.get_text(html: bool) -> str
Returns the text of the cell.
- html (bool, optional): When True the text is returned in html formatting. The default value is False. Returns -------
Returns: str

TableCell.get_text_style
ansa.report.TableCell.get_text_style(position: int) -> dict
Returns the text style in the form of a python dictionary. The dictionary contains the same keys and can be applied directly to the set_text_style function. An optional position argument can be used to return the style of a specific character in the text.
- position (int, optional): Index of the character in the text whose style is returned. Indexing starts at 1. If undefined the style of the last character is returned. Returns -------
Returns: dict Python dictionary with the following text style options.              font_family     string              font_size       integer              bold            boolean              italic          boolean              underline       boolean              strike_through  boolean              superscript     boolean              subscript       boolean              text_color      meta.windows.Color              text_align      string                                    'left'                                    'center'                                    'right'                                    'justify'              shape_align     string                                    'top'                                    'middle'                                    'bottom'                                    'top_center'                                    'middle_center'                                    'bottom_center'              autofit         string                                    'no_autofit'                                    'shrink_text_on_overflow'                                    'resize_shape_to_fit_text'              wrap            boolean

TableCell.set_border_style
ansa.report.TableCell.set_border_style(side: str, dash: str, width: float, color: windows.Color) -> bool
Sets the style of the cell's left, right, top, or bottom border.
- side (str): One of the following: "left", "right", "top", "bottom".
- dash (str, optional): One of the following: 'no_line' 'solid_line' 'dash_line' 'dot_line' 'dash_dot_line' 'lg_dash_line' 'lg_dash_dot_line' 'lg_dash_dot_dot_line' 'sys_dash_line' 'sys_dash_dot_line' 'sys_dash_dot_dot_line' 'sys_dot_line'
- width (float, optional): Border line width.
- color (windows.Color, optional): Border line color. Returns -------
Returns: bool

TableCell.set_column_width
ansa.report.TableCell.set_column_width(width: float, unit: str) -> bool
Set the column width. Takes an optional unit argument.
- width (float): Column width.
- unit (str, optional): Unit of the given width. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

TableCell.set_fill
ansa.report.TableCell.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

TableCell.set_margins
ansa.report.TableCell.set_margins(left: float, right: float, top: float, bottom: float, unit: str) -> bool
Sets the margin between the text and the edges of the cell.
- left (float, optional): Left margin.
- right (float, optional): Right margin.
- top (float, optional): Top margin.
- bottom (float, optional): Bottom margin.
- unit (str, optional): Unit of the given margins. Available values are listed below. The default value is 'mm'. - 'cm' - 'mm' - 'inch' - 'pt' - 'pixel' Returns -------
Returns: bool

TableCell.set_row_height
ansa.report.TableCell.set_row_height(height: float, unit: str) -> bool
Set the row height. Takes an optional unit argument.
- height (float): Row height.
- unit (str, optional): Unit of the given height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

TableCell.set_text
ansa.report.TableCell.set_text(text: str) -> bool
Set the text. Accepts html for formatting.
- text (str): Returns -------
Returns: bool

TableCell.set_text_style
ansa.report.TableCell.set_text_style(font_family: str, font_size: int, bold: bool, italic: bool, underline: bool, strike_through: bool, superscript: bool, subscript: bool, text_color: windows.Color, text_align: str, shape_align: str) -> bool
Set text style options. All arguments are optional. Any number of them can be used.
- font_family (str, optional): See corresponding setting in GUI for the available fonts.
- font_size (int, optional): See corresponding setting in GUI for size values.
- bold (bool, optional): 
- italic (bool, optional): 
- underline (bool, optional): 
- strike_through (bool, optional): 
- superscript (bool, optional): 
- subscript (bool, optional): 
- text_color (windows.Color, optional): meta.windows.Color
- text_align (str, optional): The following values are supported: 'left' 'center' 'right' 'justify'
- shape_align (str, optional): The following values are supported: 'top' 'middle' 'bottom' 'top_center' 'middle_center' 'bottom_center' Returns -------
Returns: bool

TableRange
ansa.report.TableRange(report_name: str, slide_name: str, shape_name: str, top_left: list, bottom_right: list, attached: bool = False) -> None
A range of cells on a table in a report in META. The range is a contiguous rectangle of cells. It is represented by the minimum row and column of the cells (top left corner), and the maximum row and column (bottom right corner).
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    # Second handler for the same textbox using constructor
    range2 = report.TableRange(rep.name, slide.name, table.name, [1, 1], [2, 2])
    print(range2)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    rep = range.get_report()
    print(rep)
    print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    slide = range.get_slide()
    print(slide)


if __name__ == "__main__":
    main()
# method: get_shape
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    table_range = range.get_shape()
    print(table_range)
    print(table_range.name)
    print(table_range.slide_name)
    print(table_range.report_name)


if __name__ == "__main__":
    main()
# method: set_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    text = "text"
    ret = range.set_text(text)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_text_style
# PYTHON script
import ansa
from ansa import report
from ansa import windows


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    ret = range.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    # ret = range.set_text_style(italic = True, underline = True, strike_through=True, superscript=True, subscript = True)
    # color = windows.Color(r = 255, g = 255, b = 0, a = 255)
    # ret = range.set_text_style(text_color = color, text_align  = 'left', shape_align = 'top', autofit = 'no_autofit', wrap = True )
    print(ret)


if __name__ == "__main__":
    main()
# method: set_row_height
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    height = 0.3
    ret = range.set_row_height(height)
    # u = 'emu'
    # ret = range.set_row_height(height, unit = u)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_column_width
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    width = 0.3
    ret = range.set_column_width(width)
    # u = 'emu'
    # ret = range.set_column_width(width, unit = u)
    print(ret)


if __name__ == "__main__":
    main()
# method: merge
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    ret = range.merge()
    print(ret)


if __name__ == "__main__":
    main()
# method: split
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    ret = range.split()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "solid_fill"
    ret = range.set_fill(fill)
    # ret =range.set_fill(fill, color=c)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_margins
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    rows = 3
    columns = 2
    table = slide.add_table(rows, columns)
    top_left = [1, 1]
    bottom_right = [2, 2]
    range = table.get_range(top_left, bottom_right)
    text = "text"
    ret = range.set_text(text)
    print(ret)
    ret = range.set_margins(left=2, top=2)
    ret = range.set_margins(right=2, bottom=2)
    # u = 'cm'
    # u = 'mm'
    # u = 'inch'
    # u = 'pt'
    # u = 'pixel'
    # ret = range.set_margins(right=2, bottom=2, unit  = u)
    print(ret)


if __name__ == "__main__":
    main()

TableRange.get_report
ansa.report.TableRange.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

TableRange.get_shape
ansa.report.TableRange.get_shape() -> Table
Return the table that this range belongs to.
Returns: Table meta.report.Table

TableRange.get_slide
ansa.report.TableRange.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

TableRange.merge
ansa.report.TableRange.merge() -> bool
Merge the cells that belong to this range.
Returns: bool

TableRange.set_column_width
ansa.report.TableRange.set_column_width(width: float, unit: str) -> bool
Set the column width. Takes an optional unit argument.
- width (float): Column width.
- unit (str, optional): Unit of the given width. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

TableRange.set_fill
ansa.report.TableRange.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

TableRange.set_margins
ansa.report.TableRange.set_margins(left: float, right: float, top: float, bottom: float, unit: str) -> bool
Sets the margin between the text and the edges of each cell, for all cells in the range.
- left (float, optional): Left margin.
- right (float, optional): Right margin.
- top (float, optional): Top margin.
- bottom (float, optional): Bottom margin.
- unit (str, optional): Unit of the given margins. Available values are listed below. The default value is 'mm'. - 'cm' - 'mm' - 'inch' - 'pt' - 'pixel' Returns -------
Returns: bool

TableRange.set_row_height
ansa.report.TableRange.set_row_height(height: float, unit: str) -> bool
Set the row height. Takes an optional unit argument.
- height (float): Row height.
- unit (str, optional): Unit of the given height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

TableRange.set_text
ansa.report.TableRange.set_text(text: str) -> bool
Set the text for every cell in the range. Accepts html for formatting.
- text (str): Returns -------
Returns: bool

TableRange.set_text_style
ansa.report.TableRange.set_text_style(font_family: str, font_size: int, bold: bool, italic: bool, underline: bool, strike_through: bool, superscript: bool, subscript: bool, text_color: windows.Color, text_align: str, shape_align: str) -> bool
Set text style options. All arguments are optional. Any number of them can be used.
- font_family (str, optional): See corresponding setting in GUI for the available fonts.
- font_size (int, optional): See corresponding setting in GUI for size values.
- bold (bool, optional): 
- italic (bool, optional): 
- underline (bool, optional): 
- strike_through (bool, optional): 
- superscript (bool, optional): 
- subscript (bool, optional): 
- text_color (windows.Color, optional): meta.windows.Color
- text_align (str, optional): The following values are supported: 'left' 'center' 'right' 'justify'
- shape_align (str, optional): The following values are supported: 'top' 'middle' 'bottom' 'top_center' 'middle_center' 'bottom_center' Returns -------
Returns: bool

TableRange.split
ansa.report.TableRange.split() -> bool
Split the cells that belong to this range if they have been merged.
Returns: bool

TextOfReportTextbox
ansa.report.TextOfReportTextbox(slide_name:str, textbox_name:str, text_type:int) -> str
This function gets the text of a textbox of a given toolbar.
- slide_name (str): Name of the slide
- textbox_name (str): Name of the textbox
- text_type (int): If value of argument “text_type” is 0 then the text after the quantification of any variables is returned, else the original text is returned.
Returns: Upon success, it returns a string referring to the text of the textbox of the given slide.
Upon failure, it returns an empty string.

Textbox
ansa.report.Textbox(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A textbox in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    textbox = slide.add_textbox()
    # Second handler for the same textbox using constructor
    textbox2 = report.Textbox(rep.name, slide.name, textbox.name)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    textbox = slide.add_textbox()
    name = "textbox name"
    ret = textbox.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    rep = shape.get_report()
    print(rep, rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    sld = shape.get_slide()
    print(sld, sld.name, sld.report_name)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    pos = shape.get_position()
    # unit = 'emu'
    # unit = 'cm'
    # unit = 'mm'
    # unit = 'inch'
    # unit = 'pt'
    # unit = 'pc'
    # unit = 'pixel'
    # unit = 'norm'
    # pos = shape.get_position(unit )
    print(pos)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    position = (0.5, 0.5)
    ret = shape.set_position(position)  # Middle of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    textbox = slide.add_textbox()
    text = "some_text"
    textbox.set_text(text)
    size = textbox.get_size()
    print(size)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    shape.set_text_style(autofit="no_autofit")
    size = (0.25, 0.25)
    ret = shape.set_size(size)  # A quarter of the size of the slide
    # unit = 'emu'
    # unit = 'cm'
    # unit = 'mm'
    # unit = 'inch'
    # unit = 'pt'
    # unit = 'pc'
    # unit = 'pixel'
    # unit = 'norm'
    # ret = shape.set_size(size, unit)   # A quarter of the size of the slide
    return ret


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    ret = shape.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    ret = shape.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    ret = shape.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    ret = shape.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    angle = 90
    ret = shape.set_rotation(angle)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    rotation = shape.get_rotation()
    print(rotation)


if __name__ == "__main__":
    main()
# method: set_line_style
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    text = "Textbox 1"
    shape.set_text(text)
    c = Color(name="color1", r=0, g=255, b=0, a=255)
    ret = shape.set_line_style(fill="solid_line", color=c)
    # ret =shape.set_line_style(fill="solid_line", color=c, width = 10.1, dash = 'solid_line')
    # ret =shape.set_line_style(fill="solid_line", color=c, width = 10.1, dash = 'solid_line', custom_dash_pattern = '111000')
    # ret =shape.set_line_style(cap = 'flat_cap', join = 'bevel_join' )
    print(ret)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    text = "Textbox 1"
    shape.set_text(text)
    fill = "no_fill"
    # fill="solid_fill"
    # fill="picture_fill"
    ret = shape.set_fill(fill)
    # c = Color(name = "color1", r = 255, g = 0, b = 0, a = 50)
    # ret = shape.set_fill(fill, color=c)
    ret = shape.set_fill(fill, filename="/home/image.png")
    print(ret)


if __name__ == "__main__":
    main()
# method: set_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    text = "some text"
    ret = shape.set_text(text)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    text = shape.get_text()
    print(text)


if __name__ == "__main__":
    main()
# method: set_text_style
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    ret = shape.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    # ret = shape.set_text_style(italic=True, strike_through=True, superscript = True)
    # color = windows.Color(r = 255, g = 255, b = 0, a = 255)
    # ret = shape.set_text_style(text_color = color, text_align = 'left', line_spacing = 'top')
    # ret = shape.set_text_style(autofit = 'shrink_text_on_overflow', wrap = True)


if __name__ == "__main__":
    main()
# method: set_margins
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    ret = shape.set_margins(left=10, top=10)
    # u = 'cm'
    # u = 'mm'
    # u = 'inch'
    # u = 'pt'
    # u = 'pixel'
    # ret = shape.set_margins(right=10, bottom=10, unit = u)
    print(ret)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag("tag_name2", "tag_value2")
    shape.add_tag(tag)
    shape.add_tag(tag2)
    ret = shape.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag("tag_name2", "tag_value2")
    shape.add_tag(tag)
    shape.add_tag(tag2)
    tags = shape.get_tags()
    for tag in tags:
        print(tag.value, tag.name)


if __name__ == "__main__":
    main()
# method: get_text_style
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    ret = shape.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    # ret = shape.set_text_style(italic=True, strike_through=True, superscript = True, subscript = True)
    # color = windows.Color(r = 255, g = 255, b = 0, a = 255)
    # ret = shape.set_text_style(text_color = color, text_align = 'left', line_spacing = 1.2, shape_align = 'middle', autofir = 'no_autofit', wrap =True)
    print(ret)
    style = shape.get_text_style()
    print(style)
    shape2 = slide.add_textbox()
    shape2.set_text_style(**style)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    text = "some_text"
    shape.set_text(text)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    text = "some_text"
    shape.set_text(text)
    ret = shape.set_visibility(False)
    print(ret)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: get_margins
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_textbox()
    ret = shape.set_margins(left=10, top=10)
    # ret = shape.set_margins(right=10, bottom=10)
    # u = 'cm'
    # u = 'mm'
    # u = 'inch'
    # u = 'pt'
    # u = 'pixel'
    # ret = shape.set_margins(unit = u)
    margins = shape.get_margins()
    # margins = shape.get_margins(unit = u)
    print(margins)


if __name__ == "__main__":
    main()

Textbox.add_tag
ansa.report.Textbox.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Textbox.bring_forward
ansa.report.Textbox.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

Textbox.bring_to_front
ansa.report.Textbox.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

Textbox.get_margins
ansa.report.Textbox.get_margins(unit: str) -> Iterable[float]
Returns the margin between the text and the edges of the textbox.
- unit (str, optional): Unit of the returned margins. Available values are listed below. The default value is 'mm'. - 'cm' - 'mm' - 'inch' - 'pt' - 'pixel' Returns -------
Returns: Iterable[float] Array of floats with the respective margin [left, right, top, bottom]

Textbox.get_position
ansa.report.Textbox.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the textbox.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

Textbox.get_report
ansa.report.Textbox.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

Textbox.get_rotation
ansa.report.Textbox.get_rotation() -> float
Return the angle of rotation of this shape in degrees.
Returns: float

Textbox.get_size
ansa.report.Textbox.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape as a list.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Textbox.get_slide
ansa.report.Textbox.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

Textbox.get_tags
ansa.report.Textbox.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Textbox.get_text
ansa.report.Textbox.get_text(html: bool) -> str
Returns the text of the textbox. The text is html formatted.
- html (bool, optional): When True the text is returned in html formatting. The default value is False. Returns -------
Returns: str

Textbox.get_text_style
ansa.report.Textbox.get_text_style(position: int) -> dict
Returns the text style in the form of a python dictionary. The dictionary contains the same keys and can be applied directly to the set_text_style function. An optional position argument can be used to return the style of a specific character in the text.
- position (int, optional): Index of the character in the text whose style is returned. Indexing starts at 1. If undefined the style of the last character is returned. Returns -------
Returns: dict Python dictionary with the following text style options.font_family     stringfont_size       integerbold            booleanitalic          booleanunderline       booleanstrike_through  booleansuperscript     booleansubscript       booleantext_color      meta.windows.Colortext_align      string                        'left'                        'center'                        'right'                        'justify'shape_align     string                        'top'                        'middle'                        'bottom'                        'top_center'                        'middle_center'                        'bottom_center'autofit         string                        'no_autofit'                        'shrink_text_on_overflow'                        'resize_shape_to_fit_text'wrap            boolean

Textbox.get_visibility
ansa.report.Textbox.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Textbox.remove_tag
ansa.report.Textbox.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Textbox.send_backward
ansa.report.Textbox.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

Textbox.send_to_back
ansa.report.Textbox.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

Textbox.set_fill
ansa.report.Textbox.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str, optional): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

Textbox.set_line_style
ansa.report.Textbox.set_line_style(fill: str, color: windows.Color, width: float, dash: str, custom_dash_pattern: str, cap: str, join: str) -> bool
Set various line style options. All arguments are optional, any number of them can be used. The result is visible only when fill is 'solid_line'.
- fill (str, optional): The available values are: - 'no_line' - 'solid_line'
- color (windows.Color, optional): meta.windows.Color
- width (float, optional): Line width.
- dash (str, optional): One of the following predefined dash patterns: 'solid_line' 'dash_line' 'dot_line' 'dash_dot_line' 'lg_dash_line' 'lg_dash_dot_line' 'lg_dash_dot_dot_line' 'sys_dash_line' 'sys_dash_dot_line' 'sys_dash_dot_dot_line' 'sys_dot_line' 'custom_dash_line'
- custom_dash_pattern (str, optional): A series of 1's and 0's describing a dash pattern. For example '111000'.
- cap (str, optional): One of the following: 'flat_cap' 'round_cap' 'square_cap'
- join (str, optional): One of the following: 'bevel_join' 'miter_join' 'round_join' Returns -------
Returns: bool

Textbox.set_margins
ansa.report.Textbox.set_margins(left: float, right: float, top: float, bottom: float, unit: str) -> bool
Sets the margin between the text and the edges of the textbox.
- left (float, optional): Left margin.
- right (float, optional): Right margin.
- top (float, optional): Top margin.
- bottom (float, optional): Bottom margin.
- unit (str, optional): Unit of the given margins. Available values are listed below. The default value is 'mm'. - 'cm' - 'mm' - 'inch' - 'pt' - 'pixel' Returns -------
Returns: bool

Textbox.set_name
ansa.report.Textbox.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

Textbox.set_position
ansa.report.Textbox.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the textbox.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Textbox.set_rotation
ansa.report.Textbox.set_rotation(angle: float) -> bool
Set the angle of rotation of this shape in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

Textbox.set_size
ansa.report.Textbox.set_size(size: Iterable[float], unit: str) -> bool
Set the textbox width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Textbox.set_text
ansa.report.Textbox.set_text(text: str) -> bool
Set the text. Accepts plain and html formatted text.
- text (str): The text to be displayed in the textbox. It may be plain text or HTML formatted. Returns -------
Returns: bool

Textbox.set_text_style
ansa.report.Textbox.set_text_style(font_family: str, font_size: int, bold: bool, italic: bool, underline: bool, strike_through: bool, superscript: bool, subscript: bool, text_color: windows.Color, text_align: str, shape_align: str, autofit: str, wrap: bool) -> bool
Set text style options. All arguments are optional. Any number of them can be used.
- font_family (str, optional): See corresponding setting in GUI for the available fonts.
- font_size (int, optional): See corresponding setting in GUI for size values.
- bold (bool, optional): 
- italic (bool, optional): 
- underline (bool, optional): 
- strike_through (bool, optional): 
- superscript (bool, optional): 
- subscript (bool, optional): 
- text_color (windows.Color, optional): meta.windows.Color
- text_align (str, optional): The following values are supported: 'left' 'center' 'right' 'justify'
- shape_align (str, optional): The following values are supported: 'top' 'middle' 'bottom' 'top_center' 'middle_center' 'bottom_center'
- autofit (str, optional): The following values are supported: 'no_autofit' 'shrink_text_on_overflow' 'resize_shape_to_fit_text'
- wrap (bool, optional): Wrap text option. Returns -------
Returns: bool

Textbox.set_visibility
ansa.report.Textbox.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

Triangle
ansa.report.Triangle(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A triangle in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    print(triangle)
    # Second handler for the same triangle using constructor
    triangle2 = report.Triangle(
        report_name=rep.name, slide_name=slide.name, shape_name=triangle.name
    )
    print(triangle2)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    name = "triangle name"
    ret = triangle.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    rep = triangle.get_report()
    print(rep)
    print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    slide = triangle.get_slide()
    print(slide)
    print(slide.name)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    position = triangle.get_position()
    print(position)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    position = (0.5, 0.5)
    ret = triangle.set_position(position)  # Middle of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    size = triangle.get_size()
    print(size)


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    size = (0.25, 0.25)
    ret = triangle.set_size(size)  # A quarter of the size of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    ret = triangle.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    ret = triangle.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    ret = triangle.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    ret = triangle.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    angle = 90
    ret = triangle.set_rotation(angle)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    rotation = triangle.get_rotation()
    print(rotation)


if __name__ == "__main__":
    main()
# method: set_line_style
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    # c = Color(name = "color1", r = 0, g = 255, b = 0, a = 255)
    fill = "no_line"
    # fill = 'solid_line'
    ret = triangle.set_line_style(fill)
    # ret = triangle.set_line_style(fill, color=c)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "no_fill"
    # fill = 'no_fill'
    # fill = 'picture_fill'
    ret = triangle.set_fill(fill, color=c)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    size = (0.25, 0.25)
    ret = triangle.set_size(size)
    text = "text"
    ret = triangle.set_text(text)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_text
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    text = triangle.get_text()
    print(text)


if __name__ == "__main__":
    main()
# method: set_text_style
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    triangle = slide.add_triangle()
    triangle.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_triangle()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    ret = shape.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_triangle()
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    shape.add_tag(tag)
    ret = shape.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_triangle()
    name = "tag_name"
    value = "tag_value"
    tag = report.CreateReportTag(name, value)
    name = "tag_name2"
    value = "tag_value2"
    tag2 = report.CreateReportTag(name, value)
    shape.add_tag(tag)
    shape.add_tag(tag2)
    tags = shape.get_tags()
    for tag in tags:
        print(tag)


if __name__ == "__main__":
    main()
# method: get_text_style
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_triangle()
    shape.set_text_style(
        font_family="Monospace", font_size=24, bold=True, underline=True
    )
    style = shape.get_text_style()
    print(style)

    shape2 = slide.add_triangle()
    shape2.set_text_style(**style)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_triangle()
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    shape = slide.add_triangle()
    shape.set_visibility(False)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()

Triangle.add_tag
ansa.report.Triangle.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

Triangle.bring_forward
ansa.report.Triangle.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

Triangle.bring_to_front
ansa.report.Triangle.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

Triangle.get_position
ansa.report.Triangle.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

Triangle.get_report
ansa.report.Triangle.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

Triangle.get_rotation
ansa.report.Triangle.get_rotation() -> float
Return the angle of rotation of this shape in degrees.
Returns: float

Triangle.get_size
ansa.report.Triangle.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

Triangle.get_slide
ansa.report.Triangle.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

Triangle.get_tags
ansa.report.Triangle.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

Triangle.get_text
ansa.report.Triangle.get_text(html: bool) -> str
Returns the text of the shape.
- html (bool, optional): When True the text is returned in html formatting. The default value is False. Returns -------
Returns: str

Triangle.get_text_style
ansa.report.Triangle.get_text_style(position: int) -> dict
Returns the text style in the form of a python dictionary. The dictionary contains the same keys and can be applied directly to the set_text_style function. An optional position argument can be used to return the style of a specific character in the text.
- position (int, optional): Index of the character in the text whose style is returned. Indexing starts at 1. If undefined the style of the last character is returned. Returns -------
Returns: dict Python dictionary with the following text style options.font_family     stringfont_size       integerbold            booleanitalic          booleanunderline       booleanstrike_through  booleansuperscript     booleansubscript       booleantext_color      meta.windows.Colortext_align      string                        'left'                        'center'                        'right'                        'justify'shape_align     string                        'top'                        'middle'                        'bottom'                        'top_center'                        'middle_center'                        'bottom_center'autofit         string                        'no_autofit'                        'shrink_text_on_overflow'                        'resize_shape_to_fit_text'wrap            boolean

Triangle.get_visibility
ansa.report.Triangle.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

Triangle.remove_tag
ansa.report.Triangle.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

Triangle.send_backward
ansa.report.Triangle.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

Triangle.send_to_back
ansa.report.Triangle.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

Triangle.set_fill
ansa.report.Triangle.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

Triangle.set_line_style
ansa.report.Triangle.set_line_style(fill: str, color: windows.Color, width: float, dash: str, custom_dash_pattern: str, cap: str, join: str) -> bool
Set various line style options. All arguments are optional, any number of them can be used. The result is visible only when fill is 'solid_line'.
- fill (str, optional): The available values are: - 'no_line' - 'solid_line'
- color (windows.Color, optional): meta.windows.Color
- width (float, optional): Line width.
- dash (str, optional): One of the following predefined dash patterns: 'solid_line' 'dash_line' 'dot_line' 'dash_dot_line' 'lg_dash_line' 'lg_dash_dot_line' 'lg_dash_dot_dot_line' 'sys_dash_line' 'sys_dash_dot_line' 'sys_dash_dot_dot_line' 'sys_dot_line' 'custom_dash_line'
- custom_dash_pattern (str, optional): A series of 1's and 0's describing a dash pattern. For example '111000'.
- cap (str, optional): One of the following: 'flat_cap' 'round_cap' 'square_cap'
- join (str, optional): One of the following: 'bevel_join' 'miter_join' 'round_join' Returns -------
Returns: bool

Triangle.set_name
ansa.report.Triangle.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

Triangle.set_position
ansa.report.Triangle.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Triangle.set_rotation
ansa.report.Triangle.set_rotation(angle: float) -> bool
Set the angle of rotation of this shape in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

Triangle.set_size
ansa.report.Triangle.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

Triangle.set_text
ansa.report.Triangle.set_text(text: str) -> bool
Set the text. Accepts html for formatting.
- text (str): Returns -------
Returns: bool

Triangle.set_text_style
ansa.report.Triangle.set_text_style(font_family: str, font_size: int, bold: bool, italic: bool, underline: bool, strike_through: bool, superscript: bool, subscript: bool, text_color: windows.Color, text_align: str, shape_align: str, autofit: str, wrap: bool) -> bool
Set text style options. All arguments are optional. Any number of them can be used.
- font_family (str, optional): See corresponding setting in GUI for the available fonts.
- font_size (int, optional): See corresponding setting in GUI for size values.
- bold (bool, optional): 
- italic (bool, optional): 
- underline (bool, optional): 
- strike_through (bool, optional): 
- superscript (bool, optional): 
- subscript (bool, optional): 
- text_color (windows.Color, optional): meta.windows.Color
- text_align (str, optional): The following values are supported: 'left' 'center' 'right' 'justify'
- shape_align (str, optional): The following values are supported: 'top' 'middle' 'bottom' 'top_center' 'middle_center' 'bottom_center'
- autofit (str, optional): The following values are supported: 'no_autofit' 'shrink_text_on_overflow' 'resize_shape_to_fit_text'
- wrap (bool, optional): Wrap text option. Returns -------
Returns: bool

Triangle.set_visibility
ansa.report.Triangle.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

VideoShape
ansa.report.VideoShape(report_name: str, slide_name: str, shape_name: str, attached: bool = False) -> None
A video shape in a report in META.
Example:
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    # Second handler for the same video using constructor
    video2 = report.VideoShape(
        report_name=rep.name, slide_name=slide.name, shape_name=video.name
    )
    print(video2)


if __name__ == "__main__":
    main()
# method: get_report
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    rep = video.get_report()
    print(rep)
    print(rep.name)


if __name__ == "__main__":
    main()
# method: get_slide
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    slide = video.get_slide()
    print(slide)


if __name__ == "__main__":
    main()
# method: get_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    position = video.get_position()
    print(position)


if __name__ == "__main__":
    main()
# method: set_position
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    position = (0.5, 0.5)
    ret = video.set_position(position)  # Middle of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: get_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    print(video.get_size())


if __name__ == "__main__":
    main()
# method: set_size
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video.avi"
    video = slide.add_video(filename)
    size = (0.25, 0.25)
    ret = video.set_size(size)  # A quarter of the size of the slide
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_to_front
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video.avi"
    video = slide.add_video(filename)
    ret = video.bring_to_front()
    print(ret)


if __name__ == "__main__":
    main()
# method: bring_forward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    ret = video.bring_forward()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_to_back
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    ret = video.send_to_back()
    print(ret)


if __name__ == "__main__":
    main()
# method: send_backward
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    ret = video.send_backward()
    print(ret)


if __name__ == "__main__":
    main()
# method: get_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    rotation = video.get_rotation()
    print(rotation)


if __name__ == "__main__":
    main()
# method: set_rotation
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    angle = 90
    ret = video.set_rotation(angle)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_name
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    name = "video name"
    ret = video.set_name(name)
    print(ret)


if __name__ == "__main__":
    main()
# method: add_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    shape = slide.add_video(filename)
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    ret = shape.add_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: remove_tag
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    shape = slide.add_video(filename)
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    ret = shape.add_tag(tag)
    print(ret)
    ret = shape.remove_tag(tag)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_tags
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    shape = slide.add_video(filename)
    tag = report.CreateReportTag(name="tag_name", value="tag_value")
    tag2 = report.CreateReportTag(name="tag_name2", value="tag_value2")
    shape.add_tag(tag)
    shape.add_tag(tag2)
    tags = shape.get_tags()
    for tag in tags:
        print(tag)


if __name__ == "__main__":
    main()
# method: set_fill
# PYTHON script
import ansa
from ansa import report
from ansa.windows import Color


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    c = Color(name="color1", r=255, g=0, b=0, a=50)
    fill = "solid_fill"
    ret = video.set_fill(fill, color=c)
    print(ret)


if __name__ == "__main__":
    main()
# method: set_file
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    ret = video.set_file(filename)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_file
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    file = video.get_file()
    print(file)


if __name__ == "__main__":
    main()
# method: set_embed
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    embed = False
    ret = video.set_embed(embed)
    print(ret)


if __name__ == "__main__":
    main()
# method: get_embed
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    video = slide.add_video(filename)
    embed = video.get_embed()
    print(embed)


if __name__ == "__main__":
    main()
# method: get_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    shape = slide.add_video(filename)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()
# method: set_visibility
# PYTHON script
import ansa
from ansa import report


def main():
    rep = report.CreateReport()
    slide = rep.get_slides()[0]
    filename = "video1.avi"
    shape = slide.add_video(filename)
    visibility = False
    ret = shape.set_visibility(visibility)
    ret = shape.get_visibility()
    print(ret)


if __name__ == "__main__":
    main()

VideoShape.add_tag
ansa.report.VideoShape.add_tag(tag: ReportTag) -> bool
Adds a tag to the shape.
- tag (ReportTag): report.ReportTag object Returns -------
Returns: bool

VideoShape.bring_forward
ansa.report.VideoShape.bring_forward() -> bool
Place shape in front of the shape immediately in front of it.
Returns: bool

VideoShape.bring_to_front
ansa.report.VideoShape.bring_to_front() -> bool
Place shape in front of the other shapes in the slide.
Returns: bool

VideoShape.get_embed
ansa.report.VideoShape.get_embed() -> bool
Whether the video is embedded or linked.
Returns: bool True if the video file is embedded. False if the video file is linked.

VideoShape.get_file
ansa.report.VideoShape.get_file() -> str
Return the video file used by this shape.
Returns: str Path to the video file.

VideoShape.get_position
ansa.report.VideoShape.get_position(unit: str) -> Iterable[float]
Return the coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the returned coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] List that contains the x and y coordinates.

VideoShape.get_report
ansa.report.VideoShape.get_report() -> Report
Return the report that this shape belongs to.
Returns: Report meta.report.Report

VideoShape.get_rotation
ansa.report.VideoShape.get_rotation() -> float
Return the angle of rotation of this shape in degrees.
Returns: float

VideoShape.get_size
ansa.report.VideoShape.get_size(unit: str) -> Iterable[float]
Returns the width and height of the shape.
- unit (str, optional): Unit of the returned width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: Iterable[float] A list that contains the width and height in the requested units.

VideoShape.get_slide
ansa.report.VideoShape.get_slide() -> Slide
Return the slide that this shape belongs to.
Returns: Slide meta.report.Slide

VideoShape.get_tags
ansa.report.VideoShape.get_tags() -> Iterable[ReportTag]
Returns the shape tags.
Returns: Iterable[ReportTag] List of report.ReportTag objects

VideoShape.get_visibility
ansa.report.VideoShape.get_visibility() -> bool
Returns the visibility status of the shape.
Returns: bool Returns True if the shape is visible and False otherwise.

VideoShape.remove_tag
ansa.report.VideoShape.remove_tag(tag: ReportTag | str) -> bool
Removes the given tag from the shape.
- tag (ReportTag | str): Either a report.ReportTag object or a tag name. Returns -------
Returns: bool

VideoShape.send_backward
ansa.report.VideoShape.send_backward() -> bool
Place shape behind the shape immediately behind it.
Returns: bool

VideoShape.send_to_back
ansa.report.VideoShape.send_to_back() -> bool
Place shape behind the other shapes in the slide.
Returns: bool

VideoShape.set_embed
ansa.report.VideoShape.set_embed(embed: bool) -> bool
Define whether the video file will be embedded or linked on export.
- embed (bool): True if the video file is embedded. False if the video file is linked. Returns -------
Returns: bool

VideoShape.set_file
ansa.report.VideoShape.set_file(filename: str) -> bool
Set the displayed video.
- filename (str): Path to a video file. Returns -------
Returns: bool

VideoShape.set_fill
ansa.report.VideoShape.set_fill(fill: str, color: windows.Color, filename: str) -> bool
Background fill options. Fill can be either empty ('no_fill'), have a solid color fill ('solid_fill'), or have a background image ('picture_fill'). The solid color option requires a valid color argument while the background image option requires a valid filename argument.
- fill (str, optional): Supported values are: 'no_fill' 'solid_fill' 'picture_fill'
- color (windows.Color, optional): meta.windows.Color
- filename (str, optional): Path to an image file. Returns -------
Returns: bool

VideoShape.set_name
ansa.report.VideoShape.set_name(name: str) -> bool
Set the shape name.
- name (str): Returns -------
Returns: bool

VideoShape.set_position
ansa.report.VideoShape.set_position(position: Iterable[float], unit: str) -> bool
Set the top left corner position of the shape.
- position (Iterable[float]): Either a list or 2-tuple that contains the desired coordinates of the top left corner of the shape.
- unit (str, optional): Unit of the given coordinates. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

VideoShape.set_rotation
ansa.report.VideoShape.set_rotation(angle: float) -> bool
Set the angle of rotation of this shape in degrees.
- angle (float): The angle in degrees. Returns -------
Returns: bool

VideoShape.set_size
ansa.report.VideoShape.set_size(size: Iterable[float], unit: str) -> bool
Set the shape width and height.
- size (Iterable[float]): Either a list or 2-tuple that contains the width and height.
- unit (str, optional): Unit of the given width and height. Available values are listed below. The default value is 'norm'. - 'emu' - 'cm' - 'mm' - 'inch' - 'pt' - 'pc' - 'pixel' - 'norm' : Values normalized to slide dimensions Returns -------
Returns: bool

VideoShape.set_visibility
ansa.report.VideoShape.set_visibility(visibility: bool) -> bool
Sets the visibility status of the shape.
- visibility (bool): True makes the shape visible. False makes the shape hidden. Returns -------
Returns: bool

WidthOfSlide
ansa.report.WidthOfSlide(unit:str) -> float
This function returns the width of the report slide.
- unit (str): Refers to the unit of measurement of the width. Its possible values are:
- “mm” : Millimeters
- “pixel” : Pixels.
Returns: This function returns as float the width of the report slide in the given unit of measurement.
Example:
# PYTHON script
import ansa
from ansa import report

def main():
    w_mm = report.WidthOfSlide("mm")  # The width of the report slide in millimeter
    w_px = report.WidthOfSlide("pixel")  # The width of the report slide in pixels
    print("The width of the slide is " + str(w_mm) + " mm or " + str(w_px) + "px.")

if __name__ == "__main__":
    main()


WidthOfSpreadsheetColumns
ansa.report.WidthOfSpreadsheetColumns(slide_name:str, table_name:str, sheet_name:str, columns:Iterable[int], unit:str) -> Iterable[float]
This function returns the width of the specified columns for a given Excel table in the specified unit.
- slide_name (str): Name of the slide.
- table_name (str): Name of the table.
- sheet_name (str): Name of the sheet.
- columns (Iterable[int]): A list with the numbers of the columns of interest (Numbering starts from 0).
- unit (str): The unit of interest:
-pt
-mm
-pixel
Returns: The function returns a list that contains columns’ width.
